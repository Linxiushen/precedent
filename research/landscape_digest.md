# Self-evolving AI landscape digest — 2026-09-11

496 unique items discovered by 20 scouts; 58 deep-read. Full data: research/landscape.json (deep_reads[] has mechanism/eval_loop/limitations/safety/open_gaps per item).

## Deep-read projects (ranked by impact+novelty)

### Voyager: An Open-Ended Embodied Agent with Large Language Models  [paper] ★7193 | 2023-05-25 | research-prototype | impact 10 novelty 9
- URL: https://arxiv.org/abs/2305.16291
- Evolves: skills-tools, tasks-curriculum, context-memory
- Mechanism: Single-agent outer loop (voyager.py learn(), 160 iterations, 3 seeds). Each iteration: (1) CurriculumAgent (GPT-4, temp 0) proposes ONE next task from current state (biome, nearby/recently-seen blocks, entities, health/hunger, inventory, chests), the list of completed tasks and "failed tasks that are too hard", plus 5 self-asked questions answered by GPT-3.5 (cached in a Chroma vectordb). Observation fields are unlocked on a warm-up schedule; first task is hardcoded "Mine 1 wood log"; inventory-full triggers hardcoded chest tasks. (2) SkillManager retrieves top-5 skills by cosine similarity be…
- Fitness/acceptance: Per-task binary success from a GPT-4 critic (LLM judge) reading ground-truth Minecraft state after execution, plus hard signals from the environment: JS execution errors and bot.chat logs from Mineflayer. No scalar reward. A skill enters the library only after the critic says success; failed tasks are retried at most 4 rounds with critique/errors fed back, then recorded as "too hard" for the curri…
- Key insight: Make executable, composable code the unit of learned capability: store only environment-verified programs, index them by natural-language description embeddings for retrieval, and inject them back as callable helpers so new skills compose old ones — capability compounds without weight updates and without catastrophic forgetting, and the closed loop propose -> retrieve -> generate -> execute -> verify -> store is what turns an LLM into a lifelong learner.
- Open gaps: Skill library is append-only: no fitness/usage/success-rate tracking, no pruning, no re-validation of a skill after the world state it was born in changes | Name collision silently overwrites a skill (skill.py add_new_skill) with no regression test on skills that compose the old version — no versioning semantics or  | No refinement of existing skills: when a retrieved skill fails inside a new composition the fix is written as new code, never propagated back to the faulty skil | Verifier sees only final inventory/state, never the code or trajectory; the curriculum prompt explicitly forbids tasks the verifier cannot check (build/place/pl | No held-out evaluation of stored skills; transfer is only measured in the same game with a new seed, no cross-domain or cross-API transfer

### AlphaEvolve: A coding agent for scientific and algorithmic discovery  [paper] ★- | 2025-05-14 | production | impact 10 novelty 8
- URL: https://arxiv.org/abs/2506.13131
- Evolves: other, prompts, harness-infrastructure
- Mechanism: The evolved object is user-marked code (# EVOLVE-BLOCK-START/END) inside a skeleton that exposes a Python evaluate() returning a dict of scalar metrics (maximized). Everything else in the file is fixed. The controller loop (asyncio, throughput-optimized) is literally: parent, inspirations = database.sample(); prompt = prompt_sampler.build(parent, inspirations); diff = llm.generate(prompt); child = apply_diff(parent, diff); results = evaluator.execute(child); database.add(child, results). Prompts contain several prior programs with their scores, rendered evaluation outputs, human-written contex…
- Fitness/acceptance: Signal is a user-written, automatically executable evaluator h returning scalar metrics; selection = registration into the MAP-Elites/island database (no notion of accept/reject beyond scores). Drift/hacking control is entirely delegated to evaluator design plus post-hoc human verification: matmul decompositions are rounded and verified exactly; Borg heuristic is correct by construction (only rank…
- Key insight: With a frozen frontier LLM as the mutation operator, an evolutionary program database (MAP-Elites + islands) plus a cheap, automatically executable evaluator is enough test-time-compute structure to turn 'thousands' of samples into SOTA-breaking discoveries — and the highest-leverage move is to evolve the search algorithm that produces the solution (with a time budget and the previous best as input) rather than the solution itself.
- Open gaps: No cross-problem transfer: each run starts from an empty program database; discovered heuristics, ideas, and meta-prompts are not stored as reusable skills/memo | Evaluator is static and human-written; the loop exploited numerical artifacts and formulation loopholes (Tao et al. 2025) — no automatic evaluator hardening, ad | Only the target code and meta-prompts evolve; the harness itself (database sampling policy, island/MAP-Elites config, model mix, cascade thresholds, prompt-samp | Model weights never update from the evolutionary trace; no RL/fine-tuning on successful diffs (contrast ThetaEvolve, Surina et al.), so the mutation operator do | Abstraction choice (evolve raw solution vs constructor vs search algorithm vs co-evolution) and the expert problem context are manual and decisive — no automati

### Absolute Zero Reasoner (AZR)  [repo] ★1904 | 2025-05-06 | research-prototype | impact 9 novelty 8
- URL: https://github.com/LeapLabTHU/Absolute-Zero-Reasoner
- Evolves: model-weights, tasks-curriculum
- Mechanism: One policy π plays two roles over three code-task types (deduction: predict o from (p,i); abduction: predict i from (p,o); induction: synthesize p from ≤10 (i,o) pairs + message). Each training step (verified in azr_ray_trainer.fit): for each task type, (1) PROPOSE: prompt π with K=6 reference triplets uniformly sampled from that type's buffer (K=1 program for induction) to write a new, "sufficiently different, challenging" program+input; (2) validate via Python executor: AST check, banned-module list (random, os, sys, time, hashlib, threading...), 10s timeout, determinism check by executing t…
- Fitness/acceptance: Fitness is entirely executor-grounded: solver gets binary Python value-equality against ground truth produced by running the proposed program; proposer gets 1 - Monte-Carlo solve rate (G=8 samples of current policy), zero for unsolvable, plus format penalties (-0.5/-1). Drift/hacking prevention: hard validity gates (AST, banned modules, determinism via double execution, timeout), invalid proposals…
- Key insight: A single model can bootstrap reasoning with zero external data if you (a) pick a task space whose ground truth is generated by executing the model's own proposal, so verification is free and unhackable, and (b) pay the proposer with 1 - (solver's current success rate), which auto-tunes a curriculum to the edge of competence; the same open-ended executor-verified loop transfers out of domain (code → math +15 pts).
- Open gaps: Verification is limited to deterministic Python value-equality; no path to self-generating verifiable tasks for tool use, multi-turn agents, stochastic programs | Proposer reward is instantaneous solve-rate (G=8 MC samples), not learning progress; no mechanism detects tasks that are 'hard' only because of brittle formatti | No explicit diversity/novelty pressure: intrinsic diversity/complexity rewards are implemented but disabled (coef=0); dedup and curriculum scheduling absent, bu | Task buffer is a flat list capped at 16384 that silently drops newest entries once full; no difficulty/age-aware archive, no priority replay, no cross-run or cr | No regression protection: checkpoint acceptance is not gated by held-out eval, only the latest checkpoint is kept, no rollback, no KL anchor to reference policy

### GEPA (Genetic-Pareto Reflective Evolution)  [repo] ★6518 | 2025-07-25 | production | impact 9 novelty 8
- URL: https://github.com/gepa-ai/gepa
- Evolves: prompts, skills-tools, agent-source-code, workflow-topology, other
- Mechanism: Core loop (src/gepa/core/engine.py + proposer/reflective_mutation): (1) Seed candidate = dict of named text components (prompts, code, skill files, configs) is fully evaluated on a validation set D_pareto; state stores per-candidate, per-instance score vectors. (2) Each iteration, ParetoCandidateSelector builds the per-instance Pareto front (best score on each val example), removes globally dominated candidates, and samples a parent with probability proportional to how many instances it wins (frontier_type can be per-instance, per-objective, hybrid, or cartesian). (3) A round-robin component s…
- Fitness/acceptance: Fitness = user-supplied scalar metric per example plus optional textual/structured side_info (ASI) used only by the reflection LM. Two-stage acceptance prevents drift: a cheap minibatch strict-improvement gate on the train split, then a full validation-set evaluation before a candidate enters the pool; per-instance Pareto tracking keeps specialists alive so greedy collapse does not occur (paper Ta…
- Key insight: Treat execution traces plus evaluator side-information as a textual gradient: let an LLM read why a candidate failed and rewrite it, but gate every mutation with a cheap minibatch improvement check followed by a full validation evaluation, and keep a per-instance Pareto front (not a single best) as the population so specialists survive and can be recombined component-wise via ancestry-aware merge.
- Open gaps: No persistent cross-run or cross-task memory: lessons from reflection are only embedded in the winning text; a new task or repo starts from an empty pool (gskil | Rejected proposals are discarded and not fed back to the reflector (issue #379), so the same parent can repeat identical failed mutations; no negative-example m | Fitness is a fixed user validation set with no adaptive/held-out rotation, k-fold, or drift detection (issue #133 open); optimizer can overfit val and the frame | Round-robin component selection: no credit assignment of failures to specific components (prompt vs code vs tool description) beyond what the reflection LM infe | Evaluator/judge is fixed and trusted; no co-evolution or auditing of the reward function, so reward hacking of weak LLM-judge metrics is unguarded

### DSPy (stanfordnlp/dspy) — Declarative Self-improving Python, with GEPA / SIMBA / MIPROv2 / BootstrapFinetune optimizers  [repo] ★37911 | 2023-01-09 | production | impact 9 novelty 8
- URL: https://github.com/stanfordnlp/dspy
- Evolves: prompts, agent-source-code, workflow-topology, model-weights
- Mechanism: Compile-time outer loop, not runtime self-modification: a DSPy program is Python whose LM calls are `Predict` nodes parameterized by (instruction text, few-shot demos). `optimizer.compile(program, trainset, valset)` returns a new program; the original is deep-copied, never mutated.

GEPA loop (verified in dspy/teleprompt/gepa/*.py + gepa-ai/gepa engine.py): (1) seed candidate = {predictor_name: instruction} plus {flex_path: module_src} for any `dspy.Flex` submodule; full valset eval. (2) Sample a parent stochastically from the per-validation-instance Pareto front (any candidate best on >=1 val…
- Fitness/acceptance: Signal = a user-written metric returning a float or `dspy.Prediction(score, feedback)`; the feedback string is injected verbatim into the reflection prompt (GEPA is the only optimizer that reads it; COPRO/MIPROv2 see a scalar). Drift/regression control: (a) strict-improvement acceptance on the same train minibatch before any full eval; (b) full held-out valset evaluation of every accepted candidat…
- Key insight: Treat prompts (and now module source code) as parameters of a declarative program and optimize them with feedback-rich evolutionary search: sample parents from a per-example Pareto front, mutate by having a strong LM read execution traces plus textual metric feedback, gate acceptance on the same minibatch, rank on a held-out valset — natural-language feedback is a far denser learning signal per rollout than a scalar reward, which is why it beats GRPO with 35x fewer rollouts.
- Open gaps: Purely offline compile-time: no continual loop that ingests production failures, re-optimizes, and regression-gates against the currently deployed artifact | Acceptance gate is a 3-example minibatch strict-improvement test with no statistical significance check and no separate test set beyond the valset used for sele | No cross-task/cross-program transfer: every compile starts from the seed; learned instructions/rules are not stored in a reusable skill or memory library | Metric/evaluator is fixed and human-written; nothing evolves the reward, and LLM-judge feedback is injected verbatim into the proposer with no reward-hacking de | Topology/code evolution (dspy.Flex) is experimental, confined to one sandboxed submodule, reflects only on whole-program I/O, and has no in-repo benchmark evide

### Darwin Gödel Machine (DGM)  [paper] ★2311 | 2025-05-29 | research-prototype | impact 9 novelty 8
- URL: https://arxiv.org/abs/2505.22954
- Evolves: agent-source-code, skills-tools, prompts, workflow-topology
- Mechanism: Verified against the released code (DGM_outer.py, self_improve_step.py). Archive starts with one hand-written coding agent (coding_agent.py + tools/bash.py, tools/edit.py; FM = Claude 3.5 Sonnet via Bedrock, frozen). Each generation (default 80 gens, 2 attempts/gen in parallel): (1) Parent selection from archive with P ∝ sigmoid(10·(acc−0.5)) × 1/(1+num_children) — only variants that compiled/evaluated are eligible. (2) Pick a self-improve "entry": with 25% each, a canned meta-problem (solve_empty_patches, solve_stochasticity, solve_contextlength), else one random unresolved benchmark task. (3…
- Fitness/acceptance: Signal = benchmark accuracy (resolved/submitted) on SWE-bench Verified (200-task subset, staged 10→60→200) or Polyglot subsets; official SWE-bench harness runs outside the agent's editable tree, so the core score is hard to fake, but in-house metrics (a hallucination-detection marker in logs) were hacked. Drift/hacking controls are weak and mostly human: eval_noise leeway 0.1, 'compiled' filter, D…
- Key insight: Drop the Gödel-machine requirement of proving an edit is beneficial; instead let the agent rewrite its own harness code (tools, workflow, prompts) with the FM frozen, validate every child empirically on a staged benchmark, and keep ALL working variants in an archive sampled by score×novelty so lower-scoring stepping stones can still parent breakthroughs — open-ended search over agent code, not hill-climbing.
- Open gaps: Fitness set == report set: the same 200 SWE-bench tasks drive selection and the headline number; no held-out task split inside the benchmark, and the o1 diagnos | No automated reward-hacking defense: the marker-removal exploit was only caught by humans reading diffs; evaluator, logging and hidden tests should be immutable | Extremely expensive fitness (~$22k, ~2 weeks per run; every child re-runs up to 200 SWE-bench tasks): no cheap proxy fitness, no bandit/successive-halving alloc | Single scalar objective (accuracy only): no cost, latency, token-usage, or safety objectives, no Pareto archive | Mutation only, one parent per child: no crossover/merging of successful patches from different archive lineages, no explicit reuse of discovered tools across br

### GEPA: Reflective Prompt Evolution Can Outperform Reinforcement Learning  [paper] ★6518 | 2025-07-25 | production | impact 9 novelty 8
- URL: https://arxiv.org/abs/2507.19457
- Evolves: prompts, agent-source-code, skills-tools
- Mechanism: Paper scope: only the natural-language instruction of each LLM module in a compound program evolves (the library's later optimize_anything / gskill extend the same loop to code, agent architectures and skills). Offline outer loop over a fixed metric-call budget (matched to MIPROv2-Heavy: e.g. 6,871 calls on HotpotQA; IFBench used 678 rollouts). State = a growing tree of candidate programs, each with per-instance scores on a validation set. Per iteration (verified in artifact code): (1) SELECT parent from the per-instance Pareto front: for every val instance collect the best-scoring candidates,…
- Fitness/acceptance: Signal = user-supplied task metric mu (exact match / F1 / constraint pass / LLM-judge for PUPA) plus a textual feedback function mu_f fed to the reflector ('actionable side information'). Three gates against drift: (a) child must strictly beat parent on the same 3-example minibatch (cheap, noisy), (b) accepted children get a full validation-set evaluation and only the per-instance val scores drive…
- Key insight: Execution traces plus textual evaluator feedback are a far denser learning signal than a scalar reward: an LLM reflecting on a handful of failed trajectories can write rules that generalize, and keeping a per-instance Pareto frontier of candidates (rather than the single best) preserves complementary lessons so the search does not collapse — yielding RL-beating accuracy with ~35x fewer rollouts and no gradient updates.
- Open gaps: Only module instructions evolve in the paper; no joint evolution of code, tool set, workflow topology or weights (library's optimize_anything extends this but w | No cross-task or cross-model transfer: learned lessons live inside a task/model-specific prompt and every new task or LM starts from the seed prompt; no reusabl | Fitness is a fixed offline train/val split; no continual or online mode that ingests production traces, detects drift, or re-optimizes incrementally. | Acceptance uses a 3-example minibatch with a strict-improvement rule and no statistical test; noisy accepts are filtered only by a costly full-valset eval, and  | Requires a hand-authored per-module textual feedback function (mu_f); no automatic synthesis of actionable feedback from raw traces or from scalar-only metrics.

### Automated Design of Agentic Systems (ADAS) / Meta Agent Search  [paper] ★1637 | 2024-08-15 | research-prototype | impact 9 novelty 8
- URL: https://arxiv.org/abs/2408.08435
- Evolves: agent-source-code, workflow-topology, prompts
- Mechanism: Verified from ShengranHu/ADAS `_mmlu/search.py` + `mmlu_prompt.py`. The unit of evolution is a single Python `forward(self, taskInfo)` function that composes calls to a fixed primitive `LLMAgentBase(output_fields, name, role, temperature)` (base LLM hard-coded to gpt-3.5-turbo-0125). The meta agent (gpt-4o-2024-05-13) is prompted once per generation with: the framework/utility code, a domain description + one example question, the ENTIRE archive dumped as JSON (each entry = thought, name, code, fitness string), a list of "WRONG implementation" anti-patterns, and the instruction to "maximize fi…
- Fitness/acceptance: Signal = ground-truth benchmark accuracy (F1 for DROP) of the exec'd forward() on a fixed validation slice: 128 questions (DROP/MGSM/MMLU), 32 questions x5 repeats (GPQA), small ARC subset x5 repeats; reported as median + 95% bootstrap CI string, which is the only feedback the meta agent sees (no per-example traces). No LLM judge. Drift/hacking controls: (1) the meta agent never sees validation qu…
- Key insight: Make the search space Turing-complete code (a forward() over a single FM-call primitive) and let an LLM meta-agent, conditioned on a growing archive of (design, fitness) pairs, write the next candidate — using the archive as stepping stones for in-context "reflection-driven" search rather than as a population under selection.
- Open gaps: Select and report by validation fitness with a paired/held-out protocol; the current pipeline's headline numbers are best-of-archive on the test set | Variance-aware acceptance: 128-example fitness with ±8pp CI needs racing/successive-halving or paired bootstrap vs parent before an agent is archived or promote | No regression gate or pruning: every runnable candidate is archived, so the archive accumulates collapsed agents (DROP gens 23-26 at ~10%) and the full archive  | Single scalar fitness feedback: the meta agent never sees per-example failures or traces, so it cannot do credit assignment (contrast GEPA/ACE reflective traces | No cost/latency objective or Pareto front; discovered agents are 10-50x more expensive than CoT (AFlow/EvoFlow later added this)

### Hyperagents (DGM-H: Darwin Gödel Machine with Hyperagents)  [repo] ★2722 | 2026-03-19 | research-prototype | impact 8 novelty 8
- URL: https://github.com/facebookresearch/hyperagents
- Evolves: agent-source-code, prompts, context-memory, skills-tools, workflow-topology, harness-infrastructure
- Mechanism: The unit of evolution is a git repo ('hyperagent') containing task_agent.py (initial: one FM call with prompt 'You are an agent... respond in JSON'), meta_agent.py (initial prompt is literally one line: 'Modify any part of the codebase at `{repo_path}`.'), the tool layer (bash + str_replace editor, text-based tool protocol, 40 tool-call cap per session), and a README stating the system should 'edit and enhance its own mechanisms for generating agents'. Loop (generate_loop.py): (1) select_parent from archive.jsonl with 'score_child_prop': sigmoid(10*(score - mean of top-3)) times exp(-(n_childr…
- Fitness/acceptance: Fitness = benchmark score from a fixed, human-written harness per domain (domains/harness.py + report.py): Polyglot pass rate (o3-mini task FM), paper-review accept/reject accuracy vs. ICLR-style ground truth (GPT-4o task FM), Genesis quadruped RL fitness from agent-written reward functions, IMO-GradingBench grading accuracy (o4-mini). Anti-drift measures: (a) staged eval on 10 tasks with a zero-s…
- Key insight: Put the improver inside the evolving artifact: if the meta-agent's code lives in the same git lineage as the task agent and every child inherits the parent's meta-agent, then meta-level infrastructure (persistent memory, performance trackers, eval-analysis scripts, prompt templates) becomes heritable and transfers across domains — a one-line 'modify any part of the codebase' prompt plus an archive is enough for these to emerge, whereas a frozen human-written improver (DGM/ADAS) yields imp@k of about 0 on new domains.
- Open gaps: Evaluation harness and task distribution are frozen: the agent cannot generate new tasks, curricula, or held-out tests, so a framework could co-evolve tasks/eva | Self-modified parent selection (E.5) underperforms the handcrafted rule; no method to validate/accept outer-loop changes (selection, evaluation) separately from | No regression gate or rollback: every compiling child enters the archive; a lower-scoring child is only down-weighted, and there is no diff-level attribution of | Meta-level improvements are only inherited along a single lineage — there is no crossover/merging of memory, trackers, or tools discovered in sibling branches o | Meta agent is not given the evaluation results or remaining budget in the released code (unused eval_path/iterations_left, issue #30); a framework should expose

### Hermes Agent  [repo] ★244400 | 2025-07-22 | production | impact 10 novelty 6
- URL: https://github.com/NousResearch/hermes-agent
- Evolves: skills-tools, context-memory
- Mechanism: Two mutable stores: (a) skills = SKILL.md + references/templates/scripts under ~/.hermes/skills (procedural memory, agentskills.io format, loaded on demand via skill_view); (b) memory = MEMORY.md (2,200 chars) + USER.md (1,375 chars) injected as a frozen block at session start, plus FTS5 session_search over SQLite and optional Honcho dialectic user model.

Foreground loop: system prompt orders the agent to load any relevant skill, to patch a skill that was wrong before finishing, and to record non-trivial workflows via skill_manage (create/patch/edit/write_file/delete). Memory tool errors when…
- Fitness/acceptance: There is no fitness signal. Acceptance of a self-modification is the reviewing LLM's own judgment against a prose "shape contract" in the prompt (class-level rules, no incident narration, no negative tool claims, no unresolved failures), plus heuristic scanners (prompt-injection/exfil/invisible-unicode patterns, NVIDIA SkillEvaluator tier-1 advisory scan on installs), plus an optional human approv…
- Key insight: Split "doing" from "learning" by forking the same warm-cache agent context into a tool-whitelisted background reviewer after every N turns, and make the learned artifact a provenance-tagged, versioned file whose lifecycle is governed by usage telemetry plus rollback rather than by any accuracy metric; consent (approval gates, pins, ledger) substitutes for fitness.
- Open gaps: No outcome signal: skill/memory writes are never validated against task success. A framework could replay recent sessions with and without the proposed skill (o | No regression testing of learned skills: scripts/ and references/ are never executed or checked after patch/consolidation; curator umbrella merges can silently  | Usage telemetry counts loads, not helpfulness. A user correction that follows a skill load is a free negative label that is not attributed back to the skill. | Loop-health monitoring is absent: result=skill/memory/none is logged per fork but nothing alerts when learning silently fails for weeks (issue #95976 shows 41 f | Write-rate calibration: the review prompt is biased toward acting ('be ACTIVE'); there is no adaptive threshold, cross-session dedupe, or cost/benefit gate, so 

### ACE — Agentic Context Engineering: Evolving Contexts for Self-Improving Language Models  [paper] ★1310 | 2025-10-06 | usable | impact 9 novelty 7
- URL: https://arxiv.org/abs/2510.04618
- Evolves: context-memory, prompts
- Mechanism: The evolving artifact is a text "playbook": a sectioned list of bullets, each formatted `[slug-00042] helpful=X harmful=Y :: content`. Three LLM roles (paper: all non-thinking DeepSeek-V3.1) run per training sample:
1) Generator answers the task with the full playbook in context and must cite the bullet IDs it relied on.
2) Correctness is checked (ground-truth match for FiNER/Formula; AppWorld unit-test/execution feedback; a no-GT variant uses only execution feedback). Reflector receives trajectory + feedback + the cited bullets, writes a diagnosis/lesson, and tags each cited bullet helpful/ha…
- Fitness/acceptance: Signal = per-sample task correctness (exact/normalized match vs. ground truth on FiNER/XBRL-Formula; AppWorld TGC/SGC from the environment's unit tests, or raw execution feedback in the no-GT variant) plus an LLM-judge Reflector that attributes credit/blame to individual cited bullets (helpful/harmful counters). Anti-drift measures: (a) delta-only, deterministic merges so knowledge cannot be silen…
- Key insight: Never let an LLM rewrite the whole memory: represent accumulated knowledge as append-only, individually addressable bullets with usage counters, have the model emit small deltas, and merge them deterministically — this structurally forbids context collapse and brevity bias while keeping the memory human-editable, and lets a small open model match GPT-4.1 production agents by accumulating detailed, domain-specific tactics instead of concise prompts.
- Open gaps: No pruning/forgetting actually implemented: helpful/harmful counters are recorded but DELETE/MERGE/UPDATE are TODO stubs, so playbooks bloat past 100K tokens an | No rollback or A/B verification of individual deltas: a new bullet is merged without measuring whether it helps; only offline best-checkpoint-by-val-accuracy gu | Fitness is a single in-distribution benchmark per playbook; no held-out generalisation or cross-task/cross-domain transfer of learned bullets is measured, and n | Reflector credit assignment is an unverified LLM judgment on cited bullet IDs — no consistency checks, no multi-sample voting, no detection of wrong lessons or  | Only the context evolves; the Generator/Reflector/Curator prompts, the bullet schema, the merge/dedup policy and the evaluator are fixed by hand (repo issue #37

### Meta-Harness: End-to-End Optimization of Model Harnesses  [paper] ★1545 | 2026-03-30 | usable | impact 8 novelty 7.5
- URL: https://arxiv.org/abs/2603.28052
- Evolves: agent-source-code, prompts, context-memory, workflow-topology
- Mechanism: Outer loop (Algorithm 1 / meta_harness.py): seed population = hand-written baseline harnesses (zero/few-shot, ACE, MCE; Terminus-2, Terminus-KIRA), each evaluated on a search set; results written to a filesystem D. Each iteration: (1) a single coding-agent proposer (Claude Code, Opus 4.6, effort=max, tools Read/Glob/Grep/Bash/Write/Edit/Agent) is launched with a short domain SKILL.md and a task prompt pointing at evolution_summary.jsonl (one row per past candidate: hypothesis, avg_val, delta), frontier_val.json (best per dataset/task + overall best/Pareto), and per-candidate dirs holding sourc…
- Fitness/acceptance: Signal = task benchmark score on a search/validation set (accuracy for online text classification on USPTO/S2D/LawBench; pass@1 on 250 olympiad problems with GPT-OSS-20B; Harbor pass rate on TB2). Multi-objective via Pareto dominance over accuracy vs context tokens. Drift/overfitting controls: test sets held out until a one-shot finalize step (text-class: separate results dir, run frozen after tes…
- Key insight: Give the improving agent uncompressed, selectively-accessed experience (all prior candidates' code, scores AND raw execution traces on a filesystem it greps) instead of a hand-designed search loop with compressed feedback; the ablation shows raw traces, not scores or LLM summaries, are what make code-space harness search work, and a minimal outer loop with no parent-selection rule is sufficient once the proposer is a capable coding agent.
- Open gaps: Fitness on TB2 = the same 89-task benchmark that is reported; no automatic held-out or cross-benchmark generalization gate before a candidate is accepted onto t | No statistical acceptance test: frontier replacement is strict greater-than on a 2-trial noisy score; no re-evaluation of incumbent vs challenger, no confidence | Reward-hacking defense is a string denylist (task names, /tests, verifier) plus manual inspection; no behavioral detection of eval exploitation (reading grading | Experience is never distilled: the filesystem grows unboundedly and the proposer re-reads ~82 files every iteration; no learned memory/lessons store, no cross-r | The meta-level (SKILL.md and outer-loop script) is hand-written and frozen even though the authors say it is the strongest lever; the skill itself is not optimi

### The AI Scientist-v2 (Sakana AI)  [repo] ★7126 | 2025-04-08 | research-prototype | impact 8 novelty 7
- URL: https://github.com/SakanaAI/AI-Scientist-v2
- Evolves: other, tasks-curriculum, context-memory
- Mechanism: Not a self-modifying agent: what evolves is the experiment program (Python "node" code + plotting code) for ONE research idea, within ONE run. Loop (agent_manager.py + parallel_agent.py, AIDE-derived): an AgentManager walks 4 hard-coded main stages (1 initial_implementation, 2 baseline_tuning, 3 creative_research, 4 ablation_studies), each with LLM-generated sub-stages whose goals are synthesized from a metrics/issues summary of the journal (a dynamic mini-curriculum). Each stage runs a ParallelAgent best-first tree search: fill num_workers (4) slots per step by (a) drafting roots until num_dr…
- Fitness/acceptance: Entirely LLM/VLM-judged; there is no ground-truth fitness. Signals: (1) feedback LLM parses execution output to decide is_buggy and extract a self-declared metric (the agent itself defines the metric name/direction); (2) VLM critique of generated plots marks nodes buggy for unclear labels/missing legends/misleading visuals; (3) LLM judge picks the best node across candidates using metric + analysi…
- Key insight: Decompose open-ended research into a fixed progression of stages (prototype -> tune baseline -> explore -> ablate), run an AIDE-style best-first tree search over executable experiment programs inside each stage, freeze and carry forward only the LLM/VLM-judged best node as the root of the next stage, and make figure quality a first-class fitness signal via VLM critique that marks nodes buggy — turning a template-free coding agent into an end-to-end paper producer without any learned component.
- Open gaps: No cross-run or cross-idea transfer: every run starts from scratch; no persisted skills, code snippets, failure patterns, or prompt refinements (a framework cou | Fitness is agent-defined metric + same-family LLM/VLM judgment with no held-out or external verification; nothing catches train/test leakage, seed cherry-pickin | Final AI reviewer output is write-only; reviewer scores never drive another experiment or writeup iteration (close the loop: review -> targeted new stage/sub-st | Stage structure and stage goals are hard-coded English strings tuned for HuggingFace ML experiments; the manager cannot add/remove/reorder stages or change sear | Citations are gathered but never verified against Semantic Scholar metadata; hallucinated references reach the PDF

### Meta-Harness: End-to-End Optimization of Model Harnesses  [repo] ★1545 | 2026-03-30 | research-prototype | impact 8 novelty 7
- URL: https://github.com/stanford-iris-lab/meta-harness
- Evolves: harness-infrastructure, context-memory, prompts, skills-tools
- Mechanism: Outer loop (meta_harness.py, ~600 LOC per domain) is deliberately minimal. Each iteration: (1) spawn Claude Code (Opus 4.6, effort=max, tools Read/Glob/Grep/Agent/Write/Edit/Bash) with a SKILL.md 'proposer prior' and a task prompt pointing at run directories; (2) the proposer reads evolution_summary.jsonl (one row per past candidate: score, hypothesis, delta, components), frontier_val.json (best-per-dataset/task + Pareto list), all prior candidate source files in agents/, and raw execution traces (log.jsonl per dataset/candidate, Harbor trial dirs with full trajectories) using grep/cat — media…
- Fitness/acceptance: Signal = benchmark score on a search set: mean validation accuracy over 3 classification datasets (online learning, GPT-OSS-120B) or flat pass rate over 89 TB2 tasks x 2 trials (Opus 4.6). Frontier/"best" advances only on strict improvement, but nothing is rolled back — losers simply sit in the archive as diagnostic data. Drift/overfit controls are mostly prompt-level: SKILL.md forbids dataset/tas…
- Key insight: Give the proposer agent the entire filesystem of prior candidates — source, scores, and raw execution traces — and let it grep/cat selectively (~10M tokens/iter) instead of scalar rewards or LLM summaries; ablations show summaries add nothing (34.9 vs 34.6 median) while raw traces lift median accuracy to 50.0, so 'search with selective access to diagnostic experience' is the actual mechanism, not code evolution per se.
- Open gaps: Fitness is a single search-set score accepted on any strictly positive delta with 2 trials/task — no statistical significance test, no repeated-trial confirmati | No cross-domain or cross-task transfer of discovered harness components: each run starts from a hand-written baseline and the archive is per-run/per-domain; not | No lineage/credit assignment: candidates record only a free-text 'base_system' and 'components' tags, so the proposer cannot query which mechanisms historically | Overfitting/reward-hacking defense is prompt text plus manual code reading; no automated leakage detector in the released paper loop (the experimental pilot's f | Search set == final eval set on Terminal-Bench 2; no protocol for benchmarks without a natural held-out split

### OpenViking  [repo] ★36628 | 2026-01-05 | production | impact 8.5 novelty 6.5
- URL: https://github.com/volcengine/OpenViking
- Evolves: context-memory, prompts, skills-tools, tasks-curriculum
- Mechanism: Two loops over a filesystem-shaped context DB (viking://). ONLINE: session.commit() archives messages synchronously, then asynchronously runs SessionCompressorV3 -> ExtractLoop, a <=3-iteration ReAct orchestrator where the LLM may `read`/`find` existing memory files and then emits operations (JSON or restricted-Python SEARCH/REPLACE protocol). Candidates pass a vector pre-filter + LLM dedup decision (candidate: skip/create/none; per existing item: merge/delete) and are written by MemoryUpdater with a per-commit memory_diff.json (before/after audit) and re-embedded. If the server-wide agent_evo…
- Fitness/acceptance: Online loop: no external fitness signal at all — extraction, dedup/merge and supersede decisions are single-LLM judgments (ExtractLoop with read/find tools, <=3 ReAct rounds); drift is limited only by schema prompts (abstraction mandate, atomic scope, 8-bullet cap), a base-content guard on writes, and a memory_diff.json audit trail (manual rollback). Offline loop: environment reward from a benchma…
- Key insight: Treat the agent's experience directory as a versioned, file-based Policy Set and optimize it with 'semantic gradients': rollouts -> outcome-tagged trajectories (with env reward injected as feedback) -> proposed before/after file patches -> one LLM merge/split/delete pass -> locked, version-guarded apply with an audit diff — so learned behavior stays browsable, diffable and lineage-tracked rather than hidden in a vector store or weights.
- Open gaps: Held-out eval delta is only reported, never used as an acceptance gate: no automatic rollback or rejection of a policy update that lowers test accuracy (memory_ | Outside benchmark harnesses there is no fitness signal: fallback RubricEvaluation is 'passed iff a trajectory was extracted', so real-world experience extractio | Per-experience lineage stats (success/failure counts of trajectories that read it) are surfaced only in the Studio UI; not used to deprecate, prune, down-rank a | Only memory/experience text evolves; the host harness (Claude Code, Codex, OpenClaw agent loop, tool set, system prompts) is untouched, and extracted session sk | Offline training needs a bespoke HTTP rollout/environment service with ground-truth reward (tau2); no generic way for app developers to declare verifiable cases

### HyperAgents (DGM-Hyperagents / DGM-H)  [paper] ★2722 | 2026-03-19 | research-prototype | impact 7 novelty 8
- URL: https://arxiv.org/abs/2603.19461
- Evolves: agent-source-code, prompts, context-memory, skills-tools, harness-infrastructure
- Mechanism: A hyperagent is one Python repo containing task_agent.py (initially a single FM call that returns JSON) and meta_agent.py (initially a one-line prompt "Modify any part of the codebase at {repo_path}" driving an FM — Claude Sonnet 4.5 by default, Claude 3.5 Sonnet for Polyglot parity — with bash + str_replace editor tools). DGM-H outer loop (generate_loop.py), one child per iteration, 80-200 iterations: (1) fixed parent selection over the whole archive: score = mean of per-domain val (or train) scores, sigmoid(10*(score - mean of top-3)) times a child-count penalty (paper: 1/(1+n); code: exp(-(…
- Fitness/acceptance: Signal = fixed benchmark scores on train/val splits: Polyglot pass@1 via unit tests (10+50 train tasks, 165 test); paper-review accuracy vs. real ICLR/NeurIPS accept/reject labels (100/100/100 train/val/test); Genesis Go2 walking RL policy fitness (3 then 6 repetitions; test = zero-shot torso-height task); IMO-GradingBench grade accuracy (100/100/100). Val split drives parent selection in AI-judge…
- Key insight: Put the improvement procedure inside the same editable artifact as the task solver and have each child be generated by its parent's own meta code, so selection pressure on task score also selects for better self-modification machinery (memory, trackers, planning) that transfers to unseen domains; measure that meta-ability separately with imp@k under a frozen meta agent.
- Open gaps: Outer loop (parent selection, staged gating, evaluation protocol) stays handcrafted and immutable; only a preliminary flag lets agents edit select_next_parent.p | No task/curriculum co-evolution: fitness is a fixed train/val benchmark, so improvement stalls when the benchmark saturates | No verification, regression test, or rollback for individual self-modifications beyond an import check and a benchmark score; no machine-readable admission reco | Held-out data is not isolated from the self-modifier: test samples and labels were copied into the meta agent's workspace (issue #39) | Meta agent receives no structured feedback in the released code (eval_path, iterations_left unused); a framework could inject per-task failure traces, diffs-vs-

### R-Zero: Self-Evolving Reasoning LLM from Zero Data  [paper] ★846 | 2025-08-07 | research-prototype | impact 8 novelty 7
- URL: https://arxiv.org/abs/2508.05004
- Evolves: model-weights, tasks-curriculum, reward-evaluator
- Mechanism: One base LLM (Qwen3-4B/8B-Base, OctoThinker) is copied into two separately-trained weight sets: Challenger Q and Solver S. Each iteration (scripts/main.sh) has three phases. (1) Challenger GRPO, 5 steps: a single fixed prompt ("write one new competition-math problem, <question>…</question> + \boxed{answer}") is sampled n=4–5 times per group. Reward per question: the frozen current Solver (served via vLLM Flask servers) samples m=10 answers; p = fraction agreeing with the majority answer; r = min(p, 1−p) in code (paper: 1−2|p−0.5|, peak at p=0.5) minus a repetition penalty = share of the GRPO b…
- Fitness/acceptance: Inside the loop there is NO external fitness signal: the Challenger is scored by Solver self-consistency (agreement with majority vote), and the Solver is scored by agreement with its own majority-vote pseudo-labels. Drift controls are only (a) the 0.3–0.8 agreement band, which drops questions that are trivially consistent or incoherent, (b) a BLEU-cluster repetition penalty within a GRPO batch (a…
- Key insight: Use the solver's own self-consistency as a single unified signal for three jobs at once: as the generator's reward (target p~0.5 = the solver's capability frontier), as the label (majority vote), and as the data filter (keep only the 0.3–0.8 band). This removes the need for any external verifier or seed data, but the same coupling is the failure mode: as the curriculum hardens, self-consistency stops tracking correctness and the loop drifts within ~3 iterations unless an external anchor is added.
- Open gaps: No external correctness anchor: pseudo-labels are pure majority vote, accuracy drops 79%->63% in 3 iterations; a framework could add cheap verifiers (code execu | No stopping, checkpoint-selection, or rollback rule: fixed 5/15 GRPO steps and fixed iteration count, collapse is only detected post-hoc on external benchmarks; | Single lineage, no archive or population: each iteration overwrites both roles, previous 8k-question sets are discarded (no replay/curriculum memory); a quality | Repetition penalty is only intra-batch BLEU clustering; no cross-iteration novelty measure, so the Challenger can re-converge on the same problem families over  | Domain locked to single-answer competition math via a hard-coded generator prompt; no mechanism for open-ended, multi-step, or tool-using tasks (Agent0/Dr. Zero

### MIPRO / MIPROv2 (Multi-prompt Instruction PRoposal Optimizer) — "Optimizing Instructions and Demonstrations for Multi-Stage Language Model Programs"  [paper] ★37914 | arXiv v1 2 | production | impact 8 novelty 7
- URL: https://arxiv.org/abs/2406.11695
- Evolves: prompts
- Mechanism: Setting: a multi-module LM program Φ, a scalar metric μ, a small labeled trainset; no gradients, no per-module labels. Each module i has two latent categorical variables: instruction index and few-shot-demo-set index. Three stages (verified in DSPy source):

1. Bootstrap demos: run the unoptimized Φ on train inputs; keep whole traces whose final output scores ≥ threshold (rejection sampling); slice each accepted trace into per-module input/output demos; assemble N candidate demo sets per module.

2. Grounded instruction proposal: a proposer LM (GPT-3.5/4 in paper) writes N instructions per mod…
- Fitness/acceptance: Signal = user-supplied task metric μ (exact match, accuracy, Recall@21, custom regex) averaged over stochastic minibatches of a labeled train/val split; a TPE surrogate does credit assignment across modules from these noisy scores. Drift/overfit control: periodic full-valset evaluation of the top-mean-minibatch candidate, and only fully-evaluated programs are eligible to be returned (minibatch win…
- Key insight: Decouple proposal from credit assignment: have the LM generate a grounded pool of per-module candidates once (using data summary, program code, bootstrapped traces, and a diversity tip), then let a cheap Bayesian surrogate over discrete per-module choices, fed by noisy minibatch scores of the whole pipeline and guarded by periodic full evaluations, do joint credit assignment — no gradients, log-probs, or intermediate labels required.
- Open gaps: Candidate pool is frozen before search: evaluation outcomes never rewrite instructions or demos (surrogate only selects among N pre-proposed options) — no refle | Credit assignment is purely statistical over categorical indices; textual execution traces and error messages are never used to attribute failures to a specific | Requires a labeled training set and a scalar metric; no support for unlabeled data, rich textual feedback, or environments where the reward is a rubric/judge | Sample-inefficient: ~300 minibatch trials x 35 examples plus repeated full-valset evals (10k+ program executions) for a 2-4 module program; no early stopping or | No cross-task or cross-program transfer: each compile starts from an empty surrogate; learned proposer hyperparameters (MIPRO++) are discarded rather than store

### ShinkaEvolve  [repo] ★1380 | 2025-09-17 | usable | impact 7.5 novelty 7
- URL: https://github.com/SakanaAI/ShinkaEvolve
- Evolves: other, prompts, context-memory, agent-source-code
- Mechanism: Per generation (async runner overlaps proposal and evaluation): (1) Pick an island, then a parent from its archive by weighted sampling — w_i = sigmoid(lambda*(F_i - median F)) * 1/(1+#children) — or power-law-over-rank / beam search; with prob exploitation_ratio draw from the global archive. Add 1 archive + 1 top-k "inspiration" program to context. (2) An AsymmetricUCB bandit (cost-aware blend) picks which LLM mutates; patch type sampled (diff 0.6 / full rewrite 0.3 / crossover 0.1). Prompt holds parent code, inspirations, evaluator text feedback, and every meta_rec_interval generations a 3-s…
- Fitness/acceptance: Signal is a user-written evaluate.py (run_shinka_eval) returning a scalar combined_score (maximized), a boolean correct from an optional validate_fn, public metrics (visible to the LLM), private metrics (hidden), and free-text feedback; num_runs>1 averages noisy evals. Drift/hacking control: only correct programs enter the archive and elites are never discarded (append-only population, so no rollb…
- Key insight: Sample-efficiency in LLM-driven evolution comes from search control rather than a smarter mutator: bias parent choice toward high-fitness-but-under-exploited programs, reject near-duplicate candidates by embedding similarity before paying for evaluation, and give the LLM-selection bandit an asymmetric, exponentiated reward that only counts strict improvement over the parent so it chases bold gains instead of safe tweaks.
- Open gaps: No held-out/generalization check inside the loop: fitness is one user evaluator on one objective, so evolved programs can overfit evaluator artifacts without de | The harness does not evolve itself: parent-sampling lambda/alpha, novelty threshold, island and migration settings, patch-type probabilities are static per run; | No task or objective generation: every run needs a hand-written evaluate.py and initial program, so 'open-ended' is aspirational | No cross-task transfer: archive, meta-scratchpad insights, bandit state and evolved system prompts live in one results_dir and are discarded for the next task | Meta-scratchpad recommendations are never validated: no credit assignment measuring whether a recommendation improved child fitness, so bad advice persists

### karpathy/autoresearch  [repo] ★95589 | 2026-03-06 | usable | impact 9 novelty 5
- URL: https://github.com/karpathy/autoresearch
- Evolves: other, prompts
- Mechanism: The evolving object is train.py (~630 lines: GPT model, Muon+AdamW optimizer, schedules, hyperparameters). The agent (Claude Code / any coding agent, "disable all permissions") is pointed at program.md, a ~7KB markdown spec that IS the entire harness; there is no driver code, no orchestrator, no Python loop. Setup: create branch autoresearch/<tag>, read README/prepare.py/train.py, create untracked results.tsv, run baseline unmodified. Then "LOOP FOREVER": (1) inspect git state, (2) hand-edit train.py with one idea, (3) git commit, (4) `uv run train.py > run.log 2>&1`, (5) `grep "^val_bpb:\|^pe…
- Fitness/acceptance: Signal = a single scalar, val_bpb from evaluate_bpb() in read-only prepare.py, measured once per candidate after exactly 5 minutes of training on one GPU. Acceptance = strict improvement over the current branch head; otherwise git reset (hard rollback), so the chain of kept commits is monotone in val_bpb by construction. Drift/reward-hacking prevention is almost entirely social/prose: 'do not modi…
- Key insight: Reduce the self-improvement harness to its irreducible minimum: a fixed compute budget (5 min wall clock) that makes every mutation comparable, one scalar metric owned by a read-only file, git as both archive and rollback, and a markdown 'program' as the sole orchestrator, so the human's job shifts from editing code to editing the research-org spec (program.md) that the agent runs.
- Open gaps: Eval integrity is prose-only: the agent owns train.py and can wrap evaluate_bpb, skip optimizer.step, or shrink EVAL_TOKENS; a framework should run the evaluato | Acceptance is a single noisy run with strict '<': no seed replication, no noise-floor estimate, no statistical acceptance test; a framework should estimate run- | No held-out generalization inside the loop: gains are verified only on the same pinned val shard and same 5-min/d8 regime; transfer to larger scale (d24) was ch | Single-lineage greedy hill-climb with no population or archive of near-misses: discarded ideas are never recombined, and agents converge on incremental hyperpar | No cross-run memory: results.tsv is untracked and never re-read by future runs; each session rediscovers the same dead ends. A framework should persist a struct

### Prime Agent: A Self-Improving RLM Harness (Prime Intellect)  [repo] ★20538 | 2026-05-08 | production | impact 8 novelty 6
- URL: https://github.com/PrimeIntellect-ai/prime-agent
- Evolves: prompts, context-memory, skills-tools, workflow-topology
- Mechanism: A typed, versioned state file (`harness_state.json`; session-local under `session-artifacts/<id>/harness/`, or global under `~/.prime/agent/harness/`) holds four entry kinds: `prompt` (supplemental notes only; base system prompt is immutable and `base_system_prompt` id is rejected), `memory`, `skill` (a *description* of an installed Python REPL callable — must carry `reference{type:python,import,callable}` + `arguments`; it does not write code), and `subagent` (reusable delegation specs invoked via `rlm.spawn`). Triggers: (a) user `/refine [instructions] [--global]`, (b) agent-callable `await…
- Fitness/acceptance: There is no fitness function. Acceptance = the refiner LLM's own judgment that an edit is "evidence-backed" plus structural validation (kind/action/id/required fields, python reference for skills, concurrency baseline). `expectedOutcome` is recorded as free text and never checked; no test, benchmark, or A/B run gates a refinement, and rollback is manual (by id) or LLM-initiated in a later refine.…
- Key insight: Split the agent's policy into an immutable base prompt plus a small, typed, versioned, scoped supplemental-state layer (prompt notes / memories / skill references / subagent specs) that a dedicated refiner call mutates only through evidence-citing CRUD edits applied at turn boundaries with before/after snapshots — making harness self-improvement auditable and reversible without touching weights or source code, and letting the agent itself request refinement from inside its REPL.
- Open gaps: No outcome-based acceptance: edits are applied on the refiner LLM's say-so; 'expectedOutcome' is free text never checked. A framework could attach a concrete ve | No measurement of entry utility: nothing tracks whether a memory/skill/subagent spec is ever read or whether trajectories improve after it exists (CH paper foun | No held-out / A-B evaluation of harness state — the paper has zero ablations of Continual Harness; a benchmark harness that runs the same task with and without  | Reward-hacking contamination is demonstrated (Factorio exploit persisted as a skill) and unmitigated: no independent validator, no least-privilege on what a ref | Refinement cannot produce executable code — skill entries are only references to already-installed Python callables; new tools still need the manual skill-creat

### SkillOpt (incl. SkillOpt-Sleep)  [repo] ★16865 | 2026-05-08 | usable | impact 8 novelty 6
- URL: https://github.com/microsoft/SkillOpt
- Evolves: skills-tools, prompts, context-memory, tasks-curriculum
- Mechanism: Two loops. (A) Research engine (skillopt-train): a frozen target model + a separate optimizer LLM (default GPT-5.5). Per step: (1) rollout target on a training batch (B=40) with the current SKILL.md -> scored trajectories; (2) reflect: optimizer analyzes failure and success minibatches (size 8, 16 parallel workers) and emits structured add/delete/replace edit patches; (3) hierarchical merge of patches; (4) rank and clip to L_t edits ("textual learning rate", default 4, cosine decay to floor 2); (5) apply -> candidate skill; (6) gate: roll out candidate on a held-out selection split (determinis…
- Fitness/acceptance: Signal = held-out selection-split hard score from a real verifier (exact match, executed openpyxl code compared cell-by-cell, ALFWorld success, MCQ label); soft/mixed metrics and an optional 'semantic density' bonus for imperative words are opt-in and were not used in the paper. Strict-improvement gate (ties rejected) + edit budget + rejected-edit buffer + protected slow-update field + best_skill…
- Key insight: Treat the skill file as the trainable parameters of a frozen agent and borrow optimizer discipline: bound edits per step (textual learning rate), accept only on strict held-out improvement, and feed rejected edits back as negative examples. The strict gate is what makes unattended text-space self-evolution safe (bounded downside vs a -52.8 pt collapse), and most of the gain comes from a handful (1-4) of accepted edits, not from wholesale rewrites.
- Open gaps: Gate is an aggregate mean over a small val slice; per-task regressions are accepted (gate_no_regression is opt-in and only in Sleep, absent from the research tr | Optimizer receives raw judge/regex source text and copies checks verbatim into the skill (Goodhart); no separation of semantic feedback from scoring criteria | Fitness is a single benchmark's selection split; no in-loop held-out generalization or cross-task test, transfer measured only post-hoc | Epoch-boundary slow update is force-accepted by default, bypassing the validation gate | Single skill document only; no skill library, routing, composition, or cross-task transfer of learned rules (stated future work)

### ACE: Agentic Context Engineering (ace-agent/ace)  [repo] ★1310 | 2025-10-06 | usable | impact 8 novelty 6
- URL: https://github.com/ace-agent/ace
- Evolves: context-memory, prompts
- Mechanism: The evolving artifact is a single plaintext 'playbook': markdown sections whose lines are itemized bullets `[slug-00042] helpful=X harmful=Y :: advice`. Per training sample (ace/ace.py `_train_single_sample`): (1) Generator answers with the full playbook in-prompt and self-reports which bullet_ids it used. (2) Correctness is checked against ground truth via `data_processor.answer_is_correct`. If wrong, up to `max_num_rounds` (3) Reflector→regenerate loops run; the Reflector (with GT or, in no-GT mode, only environment feedback) emits error_identification/root_cause/key_insight plus helpful/har…
- Fitness/acceptance: Signal = per-sample exact/format-normalized correctness against ground-truth labels (finance/Mind2Web) or AppWorld task-goal completion; in 'no_GT' mode the Reflector sees only environment/execution feedback but the retry loop still uses `answer_is_correct` with the target (issue #35 flags this inconsistency). Bullet-level credit is an LLM judgment (Reflector tags bullets the Generator self-report…
- Key insight: Never let the LLM rewrite its accumulated context wholesale: represent experience as an itemized, ID-addressed ledger of bullets with provenance counters, split doing (Generator), diagnosing (Reflector), and editing (Curator) into separate calls, and have the editor emit only small localized deltas — this avoids brevity bias and context collapse, makes each update cheap (tiny curator output, KV-cacheable prefix), and beats prompt optimizers like GEPA at a fraction of the rollouts.
- Open gaps: Curator cannot UPDATE/MERGE/DELETE — implement counter-driven pruning (harmful>=helpful, unused-after-N-steps) and LLM merge as first-class verified ops; helpfu | No per-delta acceptance test: every curator ADD is applied unconditionally; a framework should A/B each delta (or batch) on a held-out slice and reject/rollback | Fitness is single in-distribution benchmark accuracy; no cross-task or cross-domain transfer of playbooks, no held-out-domain generalization measurement, no sha | Requires ground-truth labels or trustworthy execution feedback; no mechanism to estimate reflection reliability or quarantine low-confidence insights (w/o GT Fi | Whole playbook is injected into every prompt; retrieval only exists as an unmerged PR (#34) and the team's own study shows top-k retrieval loses 35-41% of the g

### R-Zero: Self-Evolving Reasoning LLM from Zero Data  [repo] ★846 | 2025-08-07 | research-prototype | impact 7 novelty 7
- URL: https://github.com/Chengsong-Huang/R-Zero
- Evolves: model-weights, tasks-curriculum, reward-evaluator
- Mechanism: Two copies of one base model (Qwen3-4B/8B-Base, OctoThinker) are trained alternately with GRPO (verl/EasyR1 fork) for a fixed number of iterations (paper: 3; scripts/main.sh: 5). Per iteration: (1) Challenger phase — the current Solver is frozen and served by 4 vLLM+Flask reward servers; the Challenger is GRPO-trained for 6 steps (batch 16, n=4 rollouts) with a math-competition 'problem setter' system prompt. Reward per proposed question = min(p, 1-p) - repetition_penalty, where p = share of the Solver's 10 temp-1.0 samples agreeing with the majority answer (paper form: 1-2|p-0.5|), repetition…
- Fitness/acceptance: In-loop signal: Solver self-consistency (majority-vote share among 10 samples) drives both the Challenger reward (target p~0.5) and the Solver's pseudo-labels; the [0.3,0.8] band filter plus a BLEU-cluster repetition penalty are the only guards against degenerate questions. No ground truth anywhere in the loop. Improvement is reported via external benchmarks (AMC/Minerva/MATH-500/GSM8K/Olympiad/AI…
- Key insight: Turn the learner's own uncertainty into the curriculum signal: reward a proposer for questions where the solver's self-consistency is ~50% (max reward variance), filter training data to that band, and use the solver's majority vote as the label — you get a verifier-free co-evolution loop that works for a few iterations, but because the evaluator is the policy itself, label drift and diversity collapse are built in and bound the number of useful iterations.
- Open gaps: Evaluator is the policy itself (majority vote of the Solver): no independent verifier or held-out gate, so label accuracy decays 79%->63% and nothing detects or | No rollback or checkpoint selection: main.sh blindly chains 5 iterations even though the paper shows performance peaks at iter 2-3; needs benchmark-gated accept | No archive or population of tasks: each iteration's questions are consumed and discarded; the curriculum exists only in Challenger weights, so diversity collaps | Domain hard-wired to single-answer competition math via system prompt; no mechanism for open-ended, agentic, or tool-use tasks where majority vote is not a prox | Requires 8-GPU RL fine-tuning (verl/vLLM/FSDP) of 4B-8B base models — unusable for app developers or API-only settings; no prompt/skill-level analogue

### Your Agent May Misevolve: Emergent Risks in Self-evolving LLM Agents (Misevolution)  [paper] ★95 | 2025-09-30 | research-prototype | impact 7 novelty 7
- URL: https://arxiv.org/abs/2509.26354
- Evolves: model-weights, context-memory, skills-tools, workflow-topology
- Mechanism: This is NOT a self-evolution framework; it instruments four existing evolution loops and measures safety before vs. after, with safety never part of the objective. Formalization: agent θ=(model, memory, tools, workflow), θ'=f(θ, trajectory, feedback), f chosen to maximize cumulative task utility only. (1) Model: released checkpoints of Absolute-Zero (Qwen2.5-7B/14B Base/Coder self-proposes coding tasks; reward = learnability + Python-verified correctness; RL updates weights), AgentGen (Llama3.1-70B trained on self-generated environments/tasks), SEAgent (UI-TARS-7B, failure-driven curriculum),…
- Fitness/acceptance: Evolution in all studied systems is driven purely by task utility: AZR learnability/correctness rewards, SWE-Bench resolution, user-satisfaction scores from an evaluator agent, HumanEval pass rate for AFlow's MCTS selection, 'helpful tools rewarded' prompt. Safety is measured only externally by the authors: HarmBench-Llama-2-13b-cls, ShieldAgent (Agent-SafetyBench, 2000 cases), GPT-4o judge + rule…
- Key insight: Any evolution function whose acceptance signal is task utility alone will silently trade away safety across every evolvable component: this happens with no adversary, no parameter update (memory alone drops Qwen3-Coder-480B refusal from 99.4% to 54.4%), no harmful content in self-generated data, and via structurally innocuous mutations (an Ensemble node picks the 'more complete' malware). Therefore safety must be inside the fitness/acceptance gate, re-validated whenever a learned artifact (memory, tool, workflow node) is reused in a new context, and backed by versioned rollback, because post-hoc prompt/DPO patches recover only part of the loss (e.g. ASR 20.6->13.1 vs 0.6 initial; workflow RR 5.6->13.1 vs 36.3 initial).
- Open gaps: No studied framework (AFlow, SE-Agent, Alita-style tool loop, Absolute-Zero) includes a safety term in the mutation acceptance criterion; a harness could run a  | Rollback/versioning of self-modifications with tagged 'known-safe' checkpoints is only proposed in the Appendix B checklist, never implemented or tested. | Two-stage tool safety verification (static analysis at creation, LLM judge re-validation at reuse in the new context) is proposed but explicitly 'not tested'; n | Memory loop has no retrieval-time filtering, provenance/recency weighting, or quarantine; the 'treat memory as reference' prompt only cuts reward-hacking unsafe | No anomaly detector for abrupt memory-driven heuristic collapse (dynamic loop stayed safe until a single high-rated unreasonable refund at rounds 50-60 flipped 

### Reef (reef-infra) — continual learning infra for self-improving agents  [repo] ★954 | 2026-08-31 | research-prototype | impact 6.5 novelty 7
- URL: https://github.com/Human-Agent-Society/reef
- Evolves: model-weights, prompts, skills-tools, workflow-topology, agent-source-code, tasks-curriculum, harness-infrastructure
- Mechanism: Reef is a serving proxy plus a versioned release pipeline; the actual optimizer is a pluggable 'recipe'. Loop (Serve -> Observe -> Grow -> Commit): (1) Agents call an OpenAI/Anthropic-compatible endpoint with an `x-reef-scenario` header; every response returns an `x-reef-agent-record-id` receipt and the exchange is stored (for weight training, sampled tokens + rollout logprobs too). (2) Clients POST /reef/report with a score and/or free-text feedback referencing receipts; a processor applies a score window (`max_score`, default keeps only failures for harness evolution) and batches `batch_size…
- Fitness/acceptance: Per recipe, not universal. Harness path: a fixed, small task suite with a programmatic scorer (tutorial: 3 exact-last-line coding tasks) run paired on candidate vs incumbent; W/L/T tally with a configurable noise margin; rejected proposals are memorised (`max_rejected_history`) so the proposer stops re-proposing them; optional periodic recheck rolls back one bad publish; GEPA recipe uses a Pareto…
- Key insight: Treat self-improvement as a release pipeline rather than an algorithm: give every inference a receipt, attach feedback to receipts, express both weight updates and harness edits as candidate artifacts that go through the same propose -> paired-evaluate -> decide contract against the incumbent on fixed tasks, and publish only winners as immutable, hot-mountable releases with one-step rollback — so the optimizer (GEPA, SkillClaw, Meta-Harness, SAO) becomes a swappable plugin.
- Open gaps: Weight updates default to AlwaysSelect: no behavioural candidate-vs-incumbent gate, metric thresholds, or held-out regression check before hot-swapping trained  | Harness gate is a pooled win/loss tally over one small task list; no protected seed-suite floor, so gains on promoted tasks can mask regressions on original cap | Rollback is one step deep (only the previous tree is retained as a target) and rechecks are periodic/manual; no cross-release regression matrix or forgetting an | Gate verdicts are single-episode and stochastic (same tree scored 1.0 in traffic, 0.0 in 4/5 gate episodes); no built-in significance test, repeated confirmatio | Evaluator, proposer prompt and trigger policy are fixed config, not evolvable state (RFC #308 proposes putting them behind a floor but is unimplemented); no mec

### Huxley-Gödel Machine (HGM)  [paper] ★431 | 2025-10-24 | research-prototype | impact 7 novelty 6.5
- URL: https://arxiv.org/abs/2510.21614
- Evolves: agent-source-code, prompts, skills-tools
- Mechanism: Archive = a tree of coding-agent versions, each a git patch chain over a DGM-derived base agent (coding_agent.py forward() loop, tools/, prompts/). Budget = N benchmark task-evaluations (800 for the 60-task runs, 8000 for full SWE-bench Verified). Asynchronous loop with 20 workers; each iteration: if n_evals^α ≥ |tree| (+pending expansions), α=0.6 (UCB-Air widening rule), EXPAND, else EVALUATE. EXPAND: for every node with ≥1 success, pool the Bernoulli pass/fail outcomes of the node plus all its descendants (its clade) into (s_C, f_C); Thompson-sample Beta(τ(1+s_C), τ(1+f_C)) with τ=B/b anneal…
- Fitness/acceptance: Signal = binary resolved/unresolved from the official SWE-bench Verified (60-task DGM subset or full 500) or Polyglot harness, run in Docker with hidden tests; evaluation LLM is GPT-5-mini (SWE) / Qwen3-Coder-30B (Polyglot), expansion LLM GPT-5 / Qwen3-Coder-480B. Each outcome is a Bernoulli sample feeding Beta posteriors; clade-pooled posteriors (CMP estimate) drive which lineage to expand, per-a…
- Key insight: Score a candidate for further self-modification by the pooled downstream success of its entire lineage (clade) rather than its own benchmark score, and decouple 'spawn a child' from 'measure an existing node' so evaluation budget flows one task at a time to whichever node the posterior says is most promising — the parent's own fitness is a weak predictor of its descendants' fitness (Metaproductivity-Performance Mismatch).
- Open gaps: Only the object-level coding agent (coding_agent.py, tools/, prompts/) is mutated; the self-improvement machinery itself (search policy, CMP estimator, diagnose | Fitness is binary pass/fail on a fixed subset of one public benchmark (60 or 500 SWE-bench tasks); no held-out validation inside the loop, no task heterogeneity | No verification or rollback of self-modifications: a child enters the archive on any non-empty diff with no unit-test/static/diff-review gate; regressions are o | The diagnosis LLM is shown the SWE-bench private test patch and hidden-test logs of the training subset, leaking test knowledge into the mutation operator; a fr | No statistical rigor or replication protocol: single run per configuration, no seeds/CIs, no ablation isolating CMP from async execution, single-task evaluation

### GenericAgent (GA)  [repo] ★14167 | 2026-01-16 | usable | impact 7 novelty 6
- URL: https://github.com/lsdefine/GenericAgent
- Evolves: context-memory, skills-tools, prompts, tasks-curriculum
- Mechanism: Nothing in the model or harness code changes; what evolves is a file-based memory tree the agent edits with its own file_read/file_patch/file_write tools. Layers: L0 memory_management_sop.md (meta-rules), L1 global_mem_insight.txt (≤30-line index + RULES, injected into the system prompt at every task start and re-injected every 10 turns), L2 global_mem.txt (verified env facts), L3 memory/*.md SOPs and *.py scripts (skills), L4 raw session logs (cron-compressed every 12h by reflect/scheduler.py). Loop per task (agent_loop.py, ~100 lines): LLM turn -> tool dispatch -> anchor prompt (last 30 one-…
- Fitness/acceptance: No automated fitness signal. Acceptance of a new skill/fact is the LLM's own judgment at start_long_term_update, gated only by prompt rules: 'No Execution, No Memory' (must come from a successful tool result), no volatile state, patch-only edits, L1 hard cap of 30 lines, decision tree for layer placement. Drift control is heuristic: memory_cleanup_sop.md ROI rule for L1 lines, 'existence encoding'…
- Key insight: Evolve the informational environment, not the model: keep a hard ~30k-token budget, put only the existence of skills in an always-on ≤30-line index (the LLM is the decoder that fetches details on demand), admit to long-term memory only what a successful tool execution verified, and let skills ratchet from trajectory -> text SOP -> executable script so repeat tasks collapse from 32 LLM calls to 5.
- Open gaps: No automated verification of distilled skills: SOPs/scripts are never re-executed or tested before being trusted; a framework could replay the crystallized scri | No versioning or rollback of memory edits (patch-only rule is the sole guard); memory dir is not git-tracked, no diff review, no revert on regression | No outcome-based feedback loop actually shipped: file_access_stats.json read counts and the paper's usage-count weight adaptation are unused; nothing demotes or | No stale-skill detection: when a target site/API changes, the wrong SOP is still recalled; needs freshness checks or failure attribution to the SOP that was fol | No held-out generalization test of learned skills: evolution measured only on repeats of the same workflow; needs cross-task transfer benchmarks with memory-equ

### EvoScientist  [repo] ★4786 | 2026-01-26 | usable | impact 7 novelty 6
- URL: https://github.com/EvoScientist/EvoScientist
- Evolves: context-memory, skills-tools, prompts
- Mechanism: Two layers exist. (A) PAPER SYSTEM (not the repo code): Researcher Agent retrieves top-k_I=2 ideation-memory items by mxbai-embed cosine similarity, runs propose-review-refine tree search (21 candidates, 3 workers), ranks by Elo tournament on novelty/feasibility/relevance/clarity, expands top-1 into a proposal. Engineer Agent retrieves top-k_E=1 experimentation-memory item and runs 4-stage code tree search (budgets 20/12/12/18 attempts, 4 workers), keeping best-performing code per stage. Evolution Manager Agent then writes memory via three prompts: IDE (feasible directions from top-ranked idea…
- Fitness/acceptance: Paper: internal fitness is (i) an Elo tournament judged by the ideation LLM itself over 4 rubric dimensions (selects which ideas feed IDE), (ii) hard code-execution success within an attempt budget plus an LLM 'worse than baseline' verdict on the execution report (triggers IVE), (iii) best-performing implementation per stage (feeds ESE). External evaluation: Gemini-3-flash pairwise judge with posi…
- Key insight: Split experiential memory by what it steers: an ideation memory that records both promising directions AND validated dead-ends with a failure classification (implementation vs fundamental, decided by execution evidence), and an execution memory of reusable strategies distilled from best-performing trajectories -- then let a graph of linked observations (complements/contradicts/supersedes) be mined offline into human-reviewed skill proposals.
- Open gaps: No fitness signal on memory items: observations and synthesized skills are never scored against downstream task outcomes, so bad memories persist until a human  | Auto-skill acceptance is purely human approval plus format validation; no automated eval harness runs the proposed SKILL.md on held-out tasks before install | No rollback/versioning of memory or skill changes -- a regression caused by a stored observation cannot be traced and reverted automatically | No consolidation, decay, or conflict resolution: contradicts/supersedes edges are recorded but retrieval does not down-weight superseded or contradicted observa | Repo retrieval is keyword-ranked over markdown; the embedding retrieval the paper credits for gains is not implemented in the product

### Agent0 Series (Agent0 + Agent0-VL)  [repo] ★1258 | 2025-11-20 | research-prototype | impact 7 novelty 6
- URL: https://github.com/aiming-lab/Agent0
- Evolves: model-weights, tasks-curriculum
- Mechanism: Two copies of the same base LLM (Qwen3-4B/8B-Base) alternate in a 2-stage loop, T=3 iterations, hand-driven via shell scripts and manual checkpoint selection.

Stage 1 (curriculum agent, GRPO, 5 steps, batch 128, 4 rollouts): it emits <question>…</question> + boxed answer. Reward = format gate x [ min(p̂, 1-p̂) (uncertainty; p̂ = executor majority-vote agreement over 10 tool-integrated samples, max 4 Python turns via SandboxFusion) + 0.05 x min(#tool calls, 4) (tool-use reward, counted in the proposer's text) - within-batch BLEU-agglomerative-cluster share (repetition penalty, distance thresho…
- Fitness/acceptance: Inside the loop the only signal is self-consistency: the curriculum is rewarded for tasks where the executor's majority vote is ~50%, and the executor is rewarded for matching its own majority-vote pseudo-label. No ground truth or external verifier is used; tool execution grounds computation but never checks correctness. Anti-drift/anti-hacking measures: format gate (must emit question + boxed ans…
- Key insight: Pairing a task-proposer whose reward is the solver's uncertainty (p̂ near 0.5) with a solver that has real tools, and paying the proposer for tool-dependent tasks, creates an automatic difficulty ratchet; the proposer's self-consistency score should then be reused as a trust signal that scales both advantage magnitude and the PPO clip range when training on pseudo-labels.
- Open gaps: Fitness is pure self-consistency majority vote with no external or tool-based correctness verification, so confidently-wrong majority answers are reinforced; a  | Restricted to single-numeric-answer math (proofs and text answers are dropped in evaluate.py); no mechanism for open-ended, agentic, or multi-step environment t | Only model weights evolve; prompts, tool set (a single Python sandbox), harness, and reward functions are fixed — no tool/skill creation or workflow evolution. | No regression gate or rollback between iterations: checkpoints are picked manually and there is no in-loop held-out evaluation; gains plateau (4B: 51.9->52.5) w | Requires multi-GPU RL fine-tuning (8xA100/H200, 24h+ per curriculum stage per user report), so unusable by application developers working with API models.

### Agentic Harness Engineering (AHE)  [repo] ★879 | 2026-04-23 | research-prototype | impact 6 novelty 7
- URL: https://github.com/china-qijizhifeng/agentic-harness-engineering
- Evolves: prompts, context-memory, skills-tools, workflow-topology, agent-source-code
- Mechanism: Outer loop `evaluate -> analyze -> improve`, orchestrated by a 200KB `evolve.py`; base model frozen (GPT-5.4 high). (1) Evaluate: `harbor` runs the code agent (NexAU framework, seed = bash-only single shell tool + 10-line system prompt) with k=2 rollouts on all 89 Terminal-Bench 2 tasks in E2B sandboxes (64-96 concurrent), emitting per-task JSON traces + pass/fail. (2) Analyze: orchestrator computes task-level diff vs previous iteration (flipped / regressed / rollout_regressed) and runs `evaluate_changes()`: for each change in last iteration's `change_manifest.json` it intersects the declared…
- Fitness/acceptance: Fitness = pass@1 on the same 89 Terminal-Bench 2 tasks used for evolution (k=2 rollouts, ~3.2h/iteration); there is no train/held-out split (author confirms in issue #14 that the harness is evolved on the tasks it is scored on). Drift control is (a) falsifiable per-edit predictions checked against next-round task flips, (b) git-tracked file-level components so any edit is revertible, (c) prompt-le…
- Key insight: Treat every harness edit as a falsifiable experiment: force the optimizer to declare which tasks should flip and which are at risk, then mechanically score those predictions against the next round's per-task deltas and feed the verdicts back — this turns opaque LLM-driven "self-improvement" into an auditable, revertible, evidence-gated loop, and the ablation shows the durable gains live in tools, middleware and long-term memory, not in the system prompt (prompt-only edits regressed).
- Open gaps: No train/held-out task split: fitness is pass@1 on the same 89 tasks being optimized, so overfitting to Terminal-Bench evaluator conventions is unmeasured (a ne | Rollback is advisory: attribution verdicts (HARMFUL/INEFFECTIVE) are shown to the LLM but no code reverts a change or blocks re-adoption; auto-revert to best-ev | Regression prediction is near random (11.8% precision vs 5.6% baseline) and unattributed regressions are just listed, so there is no causal isolation of which e | Single lineage with greedy acceptance; no archive/population of harness variants, no Pareto front across task subsets, Best-of-N is n=2 and off by default | Optimizer process can write to orchestrator state (best_ever.json, exp_dir) — no integrity boundary between the evolved agent and its scorer

### CORAL (Human-Agent-Society)  [repo] ★974 | Repo creat | usable | impact 7 novelty 6
- URL: https://github.com/Human-Agent-Society/CORAL
- Evolves: other, context-memory, skills-tools, workflow-topology, reward-evaluator
- Mechanism: The evolving object is a user-supplied codebase ("other"), not the harness. `coral start` clones the repo, creates one git worktree per agent (branch coral/<id>), symlinks a shared `.coral/public/` (attempts/, notes/, skills/, roles/, agents/) into each worktree as `.claude/` (or `.codex/` etc.), and spawns N external coding-agent CLIs (Claude Code default; Codex, OpenCode, Cursor, Kiro, DeepSeek Harness, Pi). Each agent reads a generated CORAL.md and runs plan→edit→`coral eval -m "..."`→reflect. `coral eval` is the only git path: it commits, writes a pending Attempt JSON, and blocks until a s…
- Fitness/acceptance: Signal = a user-written scalar grader (TaskGrader.evaluate → ScoreBundle, direction max/min), run by a long-lived daemon in a subprocess with SIGKILL timeout, inside `git worktree add --detach <commit>` under `.coral/private/grader_checkouts/` so concurrent agent edits cannot perturb grading; results written atomically. Hidden data (answer keys) lives in `.coral/private/` (denied to agents; Docker…
- Key insight: Give a full coding agent the whole RETRIEVE→PROPOSE→EVALUATE→UPDATE loop instead of only PROPOSE (as AlphaEvolve/OpenEvolve do), then make the harness's only jobs (a) an unhackable async grader and (b) periodic heartbeat interrupts (reflect/consolidate/pivot-on-plateau) that force agents to externalize knowledge into a shared filesystem — the blog's own finding is that without the interrupts, autonomous agents fixate and never write the notes/skills that make multi-agent gains (1363→1103 cycles) possible.
- Open gaps: No validation or quarantine gate on shared notes/skills before other agents consume them — hallucinated or overfit notes propagate as team doctrine (maintainers | Fitness is a single visible scalar grader with readable source and no held-out evaluation; agents can specialize to the exact scoring code and nothing measures  | No automatic regression handling: a worse attempt is only labeled 'regressed'; no auto-revert, no best-of selection or final-artifact promotion policy — agent m | No cross-run or cross-task transfer: notes/skills/roles live in results/<run>/.coral/ and every run reseeds from static bundled templates; nothing learned in ru | Harness parameters are hand-set and static (heartbeat cadence every 1/10, plateau 5, migration every 16, prompts): the loop does not meta-optimize its own sched

### Everything Claude Code (ECC) — continuous-learning-v2.1 "instincts"  [repo] ★256188 | 2026-01-18 | usable | impact 8 novelty 5
- URL: https://github.com/affaan-m/everything-claude-code
- Evolves: context-memory, prompts, skills-tools
- Mechanism: Four stages, all file-based under ~/.local/share/ecc-homunculus/projects/<git-remote-hash>/. (1) OBSERVE: plugin PreToolUse/PostToolUse hooks (matcher .*, async, 10s timeout) run observe.sh, which appends one JSON line per tool call (tool, 5000-char-truncated input/output, session, project_id; regex secret-scrub) to observations.jsonl. Five guard layers stop it from observing subagents/its own observer (self-loop). No UserPromptSubmit hook, so user message text is never captured. (2) ANALYZE (off by default: observer.enabled=false): a per-project bash daemon (observer-loop.sh) wakes every 5 mi…
- Fitness/acceptance: No objective fitness signal. "Confidence" is a frequency bucket assigned by the Haiku observer at write time; the documented dynamics (+0.05 per confirmation, -0.1 per contradiction, -0.02/week decay) are not implemented anywhere in instinct-cli.py or hooks. The only acceptance checks are format contracts: frontmatter present, completion record emitted, 3+ occurrences as judged by the LLM. Nothing…
- Key insight: Decouple learning from the working agent: deterministic harness hooks (100% capture) feed a cheap background LLM that distills sessions into atomic, confidence-scored, scope-tagged "instincts" stored as files, which a SessionStart hook re-injects above a confidence floor — a memory-write/memory-read loop that needs no model training and lives entirely in the harness.
- Open gaps: No outcome-based fitness: confidence is LLM-estimated frequency, never tied to test pass/fail, task success, or user acceptance of an injected instinct | Confidence update rules (+0.05 confirm, -0.1 contradiction, weekly decay) are documented but unimplemented; nothing lowers or retires an instinct the user keeps | No feedback capture on application: the system never logs whether an injected instinct was followed, helped, or was contradicted in the session it was injected  | User prompt text is not observed (no UserPromptSubmit hook), so explicit corrections — the highest-signal event — are invisible to the observer | Cross-project promotion needs exact id match across independently generated ids; no semantic/embedding dedup or merge of near-duplicate instincts

### Dynamic Cheatsheet: Test-Time Learning with Adaptive Memory  [repo] ★276 | 2025-04-11 | usable | impact 7 novelty 6
- URL: https://github.com/suzgunmirac/dynamic-cheatsheet
- Evolves: context-memory, skills-tools
- Mechanism: Single black-box LM plays two roles per query, state is ONE string (the cheatsheet). Verified in dynamic_cheatsheet/language_model.py (advanced_generate):

DC-Cumulative: (1) Generator prompt = fixed solver instructions + [[CHEATSHEET]] + [[QUESTION]]. If the output contains a ```python block followed by "EXECUTE CODE!", the harness runs it via subprocess (python3 temp file, 3s timeout, no sandbox) and appends the stdout, looping until no more code. (2) Curator prompt = curator instructions + [[PREVIOUS_CHEATSHEET]] + [[QUESTION]] + [[MODEL_ANSWER]] (full generator transcript incl. execution o…
- Fitness/acceptance: There is no fitness signal in the loop. Memory updates are driven solely by the curator LM's own judgment of the generator transcript (plus incidental Python execution output the generator chose to run). Ground-truth accuracy is computed offline in run_benchmark.py purely for reporting. Nothing prevents drift or reward hacking except prompt wording ("only include solutions that have been tested an…
- Key insight: Splitting inference into a solver and a curator that rewrites a compact, structured natural-language memory after every episode is enough to get label-free test-time learning on a black-box model, but it only pays off when a cheap verifier (Python execution) is in the loop and the task stream is homogeneous; without an external acceptance signal, the memory converges to a handful of recency-biased, unverified entries rather than a growing library.
- Open gaps: No acceptance gate on memory writes: curator judges correctness by itself; a framework could gate insertions on execution-verified outcomes, self-consistency, o | Full-rewrite curation caps memory at ~5 items and silently discards older insights; needs incremental append/edit/dedup operations with stable IDs (delta update | No versioning, rollback, or A/B evaluation of memory states, so regressions (e.g. GPT-4o MMLU-Pro Physics -5pp with DC-Cu) are never detected or reverted | Learns and is evaluated on the same shuffled test stream; no held-out split, no measurement of order sensitivity, no cross-task transfer (e.g. reuse an AIME che | Single agent, single trajectory: no population/archive of alternative cheatsheets, no selection among candidates

### A Survey of Self-Evolving Agents: What, When, How, and Where to Evolve on the Path to Artificial Super Intelligence  [paper] ★1313 | v1 2025-07 | idea-only | impact 7 novelty 6
- URL: https://arxiv.org/abs/2507.21046
- Evolves: model-weights, context-memory, prompts, skills-tools, workflow-topology, agent-source-code, tasks-curriculum, reward-evaluator
- Mechanism: This is a survey, not a system: it proposes no loop of its own and runs no experiments. Its contribution is a formal frame plus a four-axis taxonomy for other people's loops.

Formalization: agent-environment interaction is a POMDP; an agent is a tuple of (architecture, model(s), context [memory + prompts], toolset). 'Self-evolution' is an update operator that rewrites one or more tuple components from observed trajectories and feedback so as to maximize cumulative utility over a task stream.

Taxonomy axes:
- WHAT to evolve: (1) model weights via self-generated supervision / env feedback (STa…
- Fitness/acceptance: None of its own. The survey catalogs signals used by others (env rewards, unit tests/SWE-bench, textual critiques, self-consistency/confidence, self-play win rates, LLM/agent-as-judge e.g. Mind2Web 2) and benchmarks (AgentBench, GAIA, SWE-bench, OSWorld, WebArena, MLE-bench, MCP-Universe, LifelongAgentBench, Agent-SafetyBench). Its normative contribution is to argue that adaptivity should be measu…
- Key insight: Treat self-evolution as an explicit update operator over a typed agent tuple (model, context, tools, architecture), classify every method by which component it rewrites, whether it fires inside a task or between tasks, and what signal drives it (reward / demonstration / population), and then evaluate not just adaptivity but retention, generalization, cost and safety over a persistent, non-resetting task stream.
- Open gaps: No executable reference implementation of the taxonomy: a framework could expose the agent tuple (model/context/tools/architecture) as typed, versioned mutable  | No benchmark or protocol for the 'retention' goal the survey calls most underserved: a persistent, non-resetting task stream that measures cumulative gain and f | No cost-aware evaluation: the survey asks for efficiency metrics but gives none; a framework could log tokens/wall-clock per evolution step and report improveme | Guardrails (audit trail, rollback, tool verification, human approval gates) are prescribed but never operationalized; none of the cataloged systems ship an immu | No treatment of when NOT to evolve: no trigger/gating policy that decides whether a task warrants a persistent update versus one-off in-context adaptation

### ReasoningBank (+ MaTTS: memory-aware test-time scaling)  [paper] ★578 | 2025-09-29 | research-prototype | impact 7 novelty 6
- URL: https://arxiv.org/abs/2509.25140
- Evolves: context-memory
- Mechanism: Closed loop per task in a stream, with frozen weights and a fixed agent scaffold. (1) Retrieve: embed the new task query (gemini-embedding-001, 3072-d; Qwen3-Embedding-8B optional), cosine-score against cached embeddings of PAST TASK QUERIES (not memory items), take the top-1 prior task (code: select_memory(n=1)) and paste its memory items (<=3) into the system prompt with an instruction to "explicitly discuss if you want to use each memory item". (2) Act: BrowserGym/WebArena agent or mini-swe-agent runs the task. (3) Self-judge: an LLM labels the trajectory success/fail with no ground truth -…
- Fitness/acceptance: Improvement signal is purely the LLM self-judge (success/fail label, no ground truth, no test execution even on SWE-Bench) feeding a one-shot memory-extraction prompt. There is NO fitness gate on the memory itself: every extracted item is appended regardless of whether it later helps, there is no held-out check, no A/B of memory vs no-memory, no rollback, no dedup/contradiction resolution, and no…
- Key insight: Store distilled, reusable reasoning strategies (title/description/content) rather than raw trajectories or success-only workflows, and mine FAILED trajectories for explicit pitfalls/recovery rules; then treat extra test-time rollouts as a source of contrastive signal for memory synthesis (memory <-> TTS synergy) rather than only as answer-selection.
- Open gaps: No fitness/acceptance test for a memory item: items are appended unconditionally; a framework should score items by downstream lift (e.g. counterfactual replay  | No consolidation: no dedup, merging, contradiction resolution, versioning, or decay -- bank grows linearly; template_id is stored 'for deduplication' but never  | Retrieval is top-1 prior TASK by query-embedding similarity, not retrieval over memory items themselves; no reranking, no usage/success statistics, no negative- | Self-judge is a single unverified LLM call (SWE-Bench: one-line 'success/fail' prompt) -- no test execution, no calibration, no confidence gating; mislabeled tr | Current-task/self-retrieval contamination not guarded (issue #17/#36 open); evaluation protocol conflates online learning on the test stream with generalization

### Gödel Agent: A Self-Referential Agent Framework for Recursive Self-Improvement  [paper] ★219 | 2024-10-06 | research-prototype | impact 6 novelty 7
- URL: https://arxiv.org/abs/2410.04444
- Evolves: agent-source-code, prompts, workflow-topology, skills-tools
- Mechanism: Single Python process; the agent IS the running module `agent_module`. Fixed parts: goal prompt (goal_prompt.md), `action_call_llm` / `action_call_json_format_llm` (explicitly protected from modification), the task's `evaluate()` harness, and the OpenAI API. Everything else — including `solver` (the policy π), `Agent.evolve` and `Agent.execute_action` (the meta-learner I), and the tool list — lives in runtime memory and is mutable.

Loop (mutual recursion, not a for-loop): `evolve()` builds messages = [goal prompt, `action_environment_aware()` summary of globals/attributes, last 10 items of `o…
- Fitness/acceptance: Signal = task accuracy (F1 for DROP) on a held-out validation subset (128 q; 32 for GPQA), sampled 50 at a time with a time-seeded shuffle, plus the 800-question test accuracy once valid acc crosses 0.94. The feedback string returned to the optimizer LLM is just "Valid Accuracy: X / Test Accuracy: Y" (the released code comments out per-sample diagnostics, though the goal prompt asks for case studi…
- Key insight: Make the agent's own runtime memory the substrate of evolution: expose inspect/exec-based read and monkey-patch write of every live function (including the improver itself) as ordinary tool calls, so the boundary between 'policy' and 'meta-learner' disappears and a single LLM tool-loop can in principle rewrite either — and the two things that actually made this work empirically were not the self-reference but (a) forcing an explicit analysis step before each edit and (b) catching every exception and feeding the traceback back as a tool result instead of crashing.
- Open gaps: No automatic accept/reject gate: a modified solver goes live regardless of its validation score; the only safeguard is a best-snapshot dump to disk, so a framew | Meta-level self-modification is possible but never demonstrated to help: the released best runs leave Agent.evolve untouched, and modifications to it were the m | Fitness = single-task accuracy on 50 sampled validation questions with test accuracy leaked into the feedback string; no held-out generalization, no cross-task  | Memory is the last 10 tool messages; there is no archive of tried modifications, their scores, or diffs, so the agent re-tries ideas and cannot do credit assign | Single lineage, no population/branching: a bad edit stalls the run; nothing like DGM's archive or tree search over agent variants.

### The Red Queen Gödel Machine (RQGM): Co-Evolving Agents and Their Evaluators  [paper] ★- | 2026-06-24 | research-prototype | impact 6 novelty 7
- URL: https://arxiv.org/abs/2606.26294
- Evolves: agent-source-code, prompts, reward-evaluator, tasks-curriculum
- Mechanism: Substrate is the Huxley-Gödel Machine archive tree, but each node is a multi-agent *workspace*: one shared codebase holding a meta-agent (edits the workspace), task agents (coder / paper writer / prover) and learned evaluators (code reviewer / paper reviewer / proof grader). Algorithm 1 loop over a budget of 12,288 binary validation outcomes: (1) UCB-Air gate: expand when N^0.6 >= |archive|; pick a parent by Thompson sampling on clade-metaproductivity (Beta posterior pooled over the subtree); the meta-agent self-modifies the workspace using ~3 training tasks as feedback; the child enters the a…
- Fitness/acceptance: Signal = binary success/failure per (node, role, task), aggregated by clade-metaproductivity for search and by an eps=0.05 Beta-posterior lower bound (best-belief) for selection. Two signal classes: evaluator-independent ground truth (Polyglot executable tests; CRAVE PR-acceptance labels; APReS human accept/reject; IMO-GradingBench human grades) and evaluator-dependent LLM-judge verdicts from the…
- Key insight: Make the evaluator a versioned, replaceable slot rather than a fixed frame: freeze it within epochs so fixed-criterion search guarantees still hold, replace it only at sparse checkpoints when a challenger beats the incumbent's conservative Beta lower bound on a held-out ground-truth anchor, and on replacement erase only the evidence that depended on the old judge so the archive re-ranks under the stricter criterion (an emergent curriculum) while anchor-based evidence persists.
- Open gaps: No official code: camlsys/godel-machines is an empty stub, so the private coder harness (16 tools), full role prompts, Polyglot split and CRAVE anchor sample ar | Requires a fixed human-labeled ground-truth anchor per domain (CRAVE, APReS, IMO grades); no mechanism to create, grow, rotate or audit anchors automatically, s | Guarantees are only per-epoch; no regret/convergence bound across evaluator transitions and no detection of oscillation or regression after a swap | Within an epoch the task agent can still Goodhart the frozen LLM judge; no collusion detection, randomized holdout queries or judge ensembles | Binary 0/1 outcomes only; graded rubrics, partial credit and continuous rewards must be thresholded

### Continual Harness: Online Adaptation for Self-Improving Foundation Agents  [paper] ★314 | 2026-05-11 | research-prototype | impact 6 novelty 7
- URL: https://arxiv.org/abs/2605.09998
- Evolves: prompts, context-memory, skills-tools, workflow-topology, model-weights
- Mechanism: Harness state H=(p,G,K,M): strategic system prompt p, sub-agent registry G (looping/one-step delegates with own tool lists and max_turns), skill library K (text heuristics or inline-Python executed via run_skill in a pseudo-sandbox), memory store M. Inner loop: VLM agent observes frame + ASCII map, acts via press_buttons and meta-tools (process_skill, run_code, process_subagent, process_memory). Outer loop (HarnessEvolver, same model as agent, text-only): after 25-step warmup, every 25 steps for first 200 then every 100 (or on-demand via the agent calling the evolve_harness tool), it loads the…
- Fitness/acceptance: For the harness loop there is no fitness signal, acceptance test, or rollback: every Refiner edit (including a wholesale prompt rewrite) is accepted unconditionally on the LLM's own reading of a 50-step trajectory window; 'effectiveness' ratings on skills are LLM self-assigned. The only mechanical signals are regex-detected tool-call errors and a run_code/run_skill ratio warning. Milestone count v…
- Key insight: Treat the whole harness (prompt, sub-agents, executable skills, memory) as a mutable CRUD-editable state that a Refiner rewrites mid-episode from a rolling trajectory window, so improvements compound within one long run and can be exported as a bootstrap artifact; but without a fitness gate this only works above a model-capability threshold.
- Open gaps: No acceptance test or rollback for harness edits: prompt rewrites and skill/sub-agent CRUD are applied unconditionally with no A/B on milestone rate, cost, or h | No regression guard against drift/forgetting: paper shows bootstrap-updating on Red discarded inherited sub-agents (6.4% reuse) and regressed; needs reuse prior | Refiner is schedule-driven (every 25/100 steps) rather than triggered by measured stagnation or uncertainty; no adaptive or budget-aware update policy | Fitness/eval is a single game family (Pokemon Red/Emerald to gym 3); no cross-game or cross-domain transfer of learned skills/sub-agents | Weight update loop requires a frontier teacher, PRM, and GPU SFT and is not released; no API-only, app-developer-usable path to weight co-learning

### AgentEvolver  [repo] ★1559 | 2025-11-13 | research-prototype | impact 6 novelty 6.5
- URL: https://github.com/modelscope/AgentEvolver
- Evolves: model-weights, tasks-curriculum, context-memory
- Mechanism: Outer loop is standard GRPO on veRL/Ray updating Qwen2.5-7B/14B-Instruct weights (lr 1e-6, batch 32, n=8 rollouts/task, KL 0.001, max 30 env steps, 8xA100). Three LLM-driven modules feed it:

1) Self-questioning (Task Manager): a human-written Environment Profile JSON (entities/attributes/operations + task_preference difficulty knobs) seeds a random-walk exploration by an external LLM (Qwen-Plus, temp 1.0; breadth-first Nb steps then depth-first Nd) against the sandboxed env_service. Trajectories are summarized into TaskObjective = {query, ground_truth action_sequence}. Curation: lexical dedup…
- Fitness/acceptance: Signal = benchmark scores on held-out AppWorld (TGC, test-normal) and BFCL v3 multi-turn val splits, avg@8/best@8, run before training and periodically; validation rollouts use no experience injection (woexp) so it measures internalized capability. Training reward: env verifier for original tasks, reference-grounded LLM judge for synthetic tasks (judge sees ground truth, actor does not — but this…
- Key insight: Turn the three bottlenecks of agentic RL (no tasks, blind exploration, sparse credit) into LLM-generated signals that are kept OUT of the gradient path: tasks come from environment exploration with executable reference solutions for grounded judging; retrieved experience is injected only at rollout and stripped before the policy update (with a looser off-policy clip) so it is distilled into weights instead of memorized; and per-step GOOD/BAD attribution is z-scored separately from the outcome reward so dense process credit cannot swamp the sparse success signal.
- Open gaps: Evaluator/teacher does not evolve: judges (Qwen3-235B, Qwen-Max) and explorer (Qwen-Plus) are fixed external APIs; no self-consistency check of judge against en | No acceptance gate or rollback: every RL step is applied regardless of validation; no best-checkpoint selection, no regression detection on held-out tasks | Experience pool is cold-start only (updated_freq: 0 in overall.yaml); no online curation, no per-experience utility scoring, no retirement of stale/harmful expe | Task curriculum is static: difficulty set by hand-tuned task_preference in the Environment Profile, not adapted to the policy's current pass rate (no targeting  | Environment Profile (entities/attributes/operations) must be hand-written per environment; no automatic schema induction from API specs or exploration

### OpenEvolve  [repo] ★7348 | 2025-05-15 | usable | impact 8 novelty 4.5
- URL: https://github.com/algorithmicsuperintelligence/openevolve
- Evolves: other, prompts
- Mechanism: A single fixed harness evolves a user-supplied artifact (a source file in any language, or a prompt text file) against a user-written evaluator; nothing about the agent itself evolves. Loop per iteration (process_parallel.py:_run_iteration_worker): (1) controller round-robins islands, snapshots the DB, samples a parent from the current island (20% uniform island pick, 70% from the elite archive, 10% random) plus inspirations (island-best + top-k by fitness + programs from perturbed neighbouring MAP-Elites cells + random island members); (2) prompt sampler builds system+user prompt containing p…
- Fitness/acceptance: Signal = user-written evaluator returning a metrics dict; fitness = metrics["combined_score"] if present else mean of numeric metrics excluding MAP-Elites feature dimensions (boolean metrics excluded since v0.3.2). Optional cascade evaluation (cheap stage1 -> stage2 -> stage3, each gated by a threshold) and optional LLM-as-judge quality scores mixed in at weight 0.1 (prompt-evolution example uses…
- Key insight: Use the LLM purely as a mutation operator and put all the 'intelligence' of the loop into the archive and the prompt: an island-partitioned MAP-Elites grid (keyed by cheap structural features like code length and edit-distance diversity plus user metrics) accepts a child only if it beats its cell incumbent, and the mutation prompt is filled with structured feedback the search already has for free: parent metrics, per-attempt improved/regressed deltas, top and diverse exemplars, and raw execution artifacts (stderr, timeouts). Elitism-by-cell rather than trust in the model is what prevents collapse and regressions.
- Open gaps: No cross-run or cross-task transfer: every run starts from a blank archive; discovered strategies, failure patterns and good prompts are not persisted as reusab | Harness does not evolve itself: system prompt, template variations, sampling ratios, feature dimensions, cascade thresholds and migration schedule are static pe | Fitness = the user's single evaluator on the same samples every time; no held-out split, no re-evaluation of noisy scores, no detection of overfitting or evalua | No sandbox or resource isolation for evaluated programs (memory_limit_mb/cpu_limit are parsed but never enforced); evolved code runs in the worker process | Single-file, single EVOLVE-BLOCK target; no multi-file/repository-scale evolution and no check that the LLM respected block markers (issue #106)

### OpenRSI / Frontis-MA1 / OpenMLE (Gym + ERL + Evo + Sandbox)  [repo] ★656 | 2026-07-31 | usable | impact 6.5 novelty 6
- URL: https://github.com/FrontisAI/OpenRSI
- Evolves: model-weights, context-memory, prompts
- Mechanism: Two nested loops on a shared action space of four program operators (Draft, Improve, Debug, Crossover). INNER LOOP (OpenMLE-Evo, test-time, per task, 12h budget): a FunSearch/REvolve-style island population vendored from Meta's AIRA-Dojo. Default config: 1 island, max 500 nodes, 800 steps, 100 generations x 5 individuals, crossover_prob 0.5, debug chains up to depth 10. Each step picks an operator, then parents by utility U = 1.0*score + 0.4*delta_vs_parent + 0.25*novelty (novelty = 1/sqrt(1+count of same auto-detected method family, e.g. lightgbm/transformers). The model emits a full program;…
- Fitness/acceptance: Fitness inside search = sandbox execution score on an agent-constructed validation split (fixed split instructions supplied per task); self-reported scores are diagnostics only. RL reward = leaderboard-normalized sandbox score with a GPT-based hack_check judge that rejects cheating/guessing code (categories valid/hack/hack_verify), a validation-vs-test gap penalty, and entropic advantage normaliza…
- Key insight: Align the training action space with the search action space: train the exact same four atomic program-evolution operators (Draft/Improve/Debug/Crossover) with execution-grounded rewards, so post-training directly improves the per-step productivity of a long-horizon evolutionary search rather than one-shot generation, and attribute gains with controlled model-swap vs harness-swap comparisons.
- Open gaps: No closed loop: the post-trained model's own search trajectories are never selected back into SFT/RL to retrain the next generation (paper shows one generation, | The improver only mutates task programs; it cannot mutate its own operators, prompts, parent-selection utility weights, or search hyperparameters, and there is  | Experience is per-task and per-run: experience cards/strategy boards are discarded between tasks, and the cross-task 'experience priors' of Evo-Max are undocume | Fitness is a single scalar sandbox score on an agent-chosen validation split; no held-out early-stopping or multi-objective (cost, runtime) selection beyond tok | Requires a self-hosted distributed sandbox service plus 8xH200 for training; no lightweight path for application developers to run the improvement loop on API m

### Dr. Zero: Self-Evolving Search Agents without Training Data  [repo] ★524 | 2026-01-11 | research-prototype | impact 6 novelty 6.5
- URL: https://github.com/facebookresearch/drzero
- Evolves: model-weights, tasks-curriculum, reward-evaluator
- Mechanism: Two agents initialized from the same base LLM (Qwen2.5-3B/7B-Instruct) co-evolve over 3 manual iterations, each = proposer RL -> data synthesis -> solver RL -> checkpoint convert. Seed prompts (process_train.py) are random 256-token Wikipedia passages plus a sampled hop count n in {1,2,3,4} (ratio 4:3:2:1); the Search-R1 dataset is downloaded only for row count, questions are discarded. Proposer: multi-turn tool-using rollout that must issue exactly n-1 searches and emit <question>/<answer>. Reward (reward_function.py) = 0.5*format + difficulty. Format = mean of [1, fraction of turns starting…
- Fitness/acceptance: Proposer fitness = format score (regex/substring checks) + difficulty score derived from 5 rollouts of the evolving solver, EM-checked against the proposer's own answer; solver fitness = EM against proposer-labeled answers via GRPO. Drift controls are weak: difficulty is zero for unsolvable (k=0) and trivial (k=5) questions, hop-ratio prior (40/30/20/10%) forces structural diversity, answer must b…
- Key insight: Score proposed tasks by the current solver's pass rate and reward exactly the 'hard but solvable' band (k=1 of 5), and you get an automatic curriculum at the solver's frontier without any labeled data; and GRPO's baseline does not need multiple samples of the same prompt: grouping single samples by a structural attribute (hop count) gives a usable baseline at a fraction of the sampling cost.
- Open gaps: No correctness verifier for proposer-generated answers: the proposer's own <answer> becomes solver ground truth with no evidence-grounded check, so label noise  | No regression guard between iterations: iter k+1 blindly resumes from iter k weights; an independent run saw 3B avg EM fall from 32.0 (iter1) to 28.4 (iter2) wi | Fitness is EM on 7 Wikipedia QA benchmarks only; no held-out generalization test to other corpora, live web search, other tools, or non-QA tasks | Difficulty estimation costs 5 full multi-turn solver rollouts per proposed question; no learned difficulty predictor or cached estimates | No dedup or coverage tracking of synthesized questions; diversity is enforced only by a hard-coded 4:3:2:1 hop-ratio prior and random seed documents

### AutoResearchClaw  [repo] ★14398 | Repo creat | usable | impact 7 novelty 5
- URL: https://github.com/aiming-lab/AutoResearchClaw
- Evolves: context-memory, skills-tools, prompts
- Mechanism: Two loops, neither touches weights or code. INNER (intra-run): a fixed 23-stage linear pipeline (topic -> literature -> hypothesis -> code -> sandbox -> analysis -> paper). Stage 12/13 run generated code in a sandbox; failures trigger targeted LLM repair, then up to 10 "iterative refine" rounds that keep the best version by a single scalar metric_key/direction (`_is_better`). Stage 15 has an LLM choose PROCEED / REFINE (back to 13) / PIVOT (back to 8) with artifact versioning. Optional multi-model debate (stages 8/14/18) and best-of-N tournament (8/9) add breadth. OUTER ("evolution"): only at…
- Fitness/acceptance: Inner loop signals: sandbox execution success, AST hard-validation (blocks identical ablations/hardcoded metrics), NaN/Inf fast-fail, improvement on one configured scalar metric, VerifiedRegistry that scrubs numbers not present in measurement records, 4-layer citation verification, LLM peer review (7-dim), and an optional PRM gate (3-vote LLM-as-judge returning +1/0/-1 at stages 5/9/15/20; -1 mark…
- Key insight: Treat every pipeline failure as a first-class artifact: extract it into a time-decayed natural-language lesson and re-inject it as a prompt overlay (or LLM-authored SKILL.md) so any LLM backbone avoids it next run without retraining - but the ablation shows this cross-run memory buys far less (-0.48 quality) than robust inner loops (debate -1.37, self-healing -40% completion), so long-horizon agents should invest in in-run repair/verification before cross-run evolution.
- Open gaps: Lesson store is scoped to run_dir/evolution; no global store aggregating lessons across artifacts/rc-* runs, so out-of-the-box runs are stateless and cross-run  | No fitness/acceptance signal on evolved artifacts: lessons accepted unconditionally, skills accepted from one LLM call; skill_effectiveness.jsonl is written but | Learned-skill injection takes the first 5 alphabetically sorted arc-* directories; once >5 skills exist newer ones are silently never used, and the library grow | Lesson extraction is keyword scoring over truncated error strings; no root-cause diagnosis, no mitigation field, no dedup of repeated identical lessons (the A-E | Only prompt-overlay text evolves; the 23-stage topology, stage prompts (prompt_patches.md is written but never read), tool set, and agent code are fixed

### SIA: Self Improving AI with Harness & Weight Updates  [repo] ★2148 | 2026-05 (a | usable | impact 6 novelty 6
- URL: https://github.com/hexo-ai/sia
- Evolves: agent-source-code, prompts, skills-tools, model-weights, reward-evaluator
- Mechanism: Three LLM roles in a strictly linear chain, `sia run --task X --max_gen N --focus {harness|weights}`. Gen 1: Meta-Agent (Claude Agent SDK / OpenHands / pydantic-ai) reads task.md, sample task descriptions, a reference_target_agent.py and a sample trajectory, and writes `gen_1/target_agent.py`. Each gen: orchestrator runs `python target_agent.py --dataset_dir --working_dir` in a per-run venv (optional Docker sandbox), which calls the target model and logs agent_execution.json; then runs the task's `evaluate.py --gen-dir` against `data/private/` to write results.json; then appends an LLM-written…
- Fitness/acceptance: Signal = whatever the task's `evaluate.py` writes to results.json (scored against data/private/), injected verbatim into the next feedback prompt; in weights mode the GRPO reward is defined by the agent-written EnvGroupBuilder. There is NO acceptance gate: gen n+1 is always derived from gen n even if n regressed; no rollback to best, no archive/population, no statistical test. "Best generation" is…
- Key insight: Treat the RL training pipeline (env, reward function, dataset builder) as just another source file the meta-loop can rewrite: by having the Feedback-Agent author `train.py` for a hosted fine-tuning API in the same rewrite-from-logs loop it uses for `target_agent.py`, weight updates become an artifact of harness evolution rather than a separate hand-built system — harness edits shape how the model searches/acts, weight edits build domain intuition prompts cannot instil.
- Open gaps: No acceptance gate or rollback: gen n+1 is always built from gen n even when n regressed; add best-so-far parent selection, McNemar/paired significance test, or | Fitness is a single task evaluator on one private split with no held-out or perturbed slice; no measurement of whether harness improvements transfer to other ta | Feedback evidence selection is positional (first 3 trajectories, 1000 chars each): no failure-driven sampling, no full failure transcripts, no clustering of err | Self-reported metrics are gameable (abstention raises accuracy; results.json can leak gold answers); need harness-owned coverage floors, gold-stripped eval summ | Infrastructure failures (429/timeouts/truncation) are conflated with reasoning failures in the score; no per-item failure taxonomy

### SkillClaw: Let Skills Evolve Collectively with Agentic Evolver  [repo] ★2590 | 2026-04-10 | usable | impact 6 novelty 6
- URL: https://github.com/AMAP-ML/SkillClaw
- Evolves: skills-tools
- Mechanism: Two components joined only by shared object storage (local FS / OSS / S3 / Nacos) under a group_id. (1) Client proxy: a local OpenAI/Anthropic/Responses-compatible API proxy that the agent (Hermes, OpenClaw, Codex, Claude Code, ...) is repointed to. Per request it appends an `<available_skills>` XML catalog (name/description/location, capped ~30k chars) to the system prompt so the model lazily `read`s SKILL.md files; it records every turn (prompt, response, tool calls/results/errors), infers `read_skills`/`modified_skills` from tool calls, optionally scores each turn with a PRM (LLM judge voti…
- Fitness/acceptance: Signal is almost entirely LLM judgment, no ground truth: (a) per-turn PRM = LLM judge majority vote on helpfulness (+1/0/-1) of the response to the instruction; (b) session-level LLM judge (0-1 weighted score) that overrides PRM; (c) the evolver LLM itself decides skip vs edit from summaries + these scores. Acceptance by default is 'direct' publish with verifier OFF, so the evolver's own output is…
- Key insight: Put the learning loop in a protocol-level proxy between the agent and the model so every real session from every user/agent/device is captured with zero user effort, group trajectories by the skill they actually touched to form natural cross-user control experiments, and let a deliberately conservative 'skill engineer' LLM choose improve / optimize-description / create / skip per group, syncing the result back to everyone through a shared versioned SKILL.md store.
- Open gaps: No execution-grounded acceptance: build a sandboxed re-execution harness that replays tool-using sessions with candidate vs deployed skill and measures task suc | Regression gate is opt-in and LLM-only; default publish is direct with verifier off — need mandatory pre-publish gating plus post-publish monitoring of per-skil | Attribution is 'skill was read', not 'skill helped': add counterfactual A/B (withhold/inject skill on matched tasks) to estimate each skill's causal value befor | Single converging skill per name for the whole group: no per-user/per-environment forks, overrides, or conditional sections; heterogeneous ports/APIs/OS facts g | Generator and judge are the same LLM family with no held-out task set — no defense against the evolver and verifier agreeing on plausible-but-wrong edits

### pi-autoresearch  [repo] ★8036 | 2026-03-11 | usable | impact 7 novelty 5
- URL: https://github.com/davebcn87/pi-autoresearch
- Evolves: other, context-memory, reward-evaluator
- Mechanism: Single pi agent, single git branch, greedy hill-climb over the TARGET CODEBASE (the "other" that evolves). Setup: the `autoresearch-create` skill makes the agent write `.auto/prompt.md` (objective, primary metric + direction, secondary monitors, files in scope, off-limits, "What's Been Tried") and `.auto/measure.sh` (emits `METRIC name=value` lines), creates a branch, calls `init_experiment`, runs and logs a baseline. Loop per iteration: agent edits code -> `run_experiment` runs measure.sh with timeout, parses METRIC lines, then runs optional `.auto/checks.sh` (tests/types/lint) -> `log_experi…
- Fitness/acceptance: Fitness = one user-defined scalar parsed from `.auto/measure.sh` output (lower or higher is better), plus secondary metrics that are monitors only. The KEEP decision is made by the agent, not the tool: `log_experiment` trusts `params.status` and never checks that the metric actually beat the current best; the only hard gates are (a) `keep` is refused when `.auto/checks.sh` failed on that run and (…
- Key insight: Split the loop into domain-agnostic harness infrastructure (run/measure/commit-or-revert/auto-resume/deterministic compaction) and a per-domain playbook the agent writes itself, and make the on-disk log + playbook the sole source of truth so the optimization loop survives context resets, restarts, and reverts - the agent's memory is files, not its context window.
- Open gaps: Harness does not enforce the acceptance rule: log_experiment commits whenever the agent says 'keep' without comparing metric to current best; a framework should | Evaluator (measure.sh) is readable and editable by the optimizing agent and is preserved across reverts; no hidden holdout benchmark, no hash/integrity check on | Single-lineage greedy hill climb: no archive/population of kept states, no parent selection, no branching from a non-best commit; swarm/orchestrator mode (#77)  | Noise handling is advisory only: no automatic repeated measurement, no A/B replay of marginal candidates, no statistical acceptance test (#69 open). | No completion semantics for bounded/binary objectives; loop keeps demanding iterations after the metric saturates (#64 open).

### A Comprehensive Survey of Self-Evolving AI Agents: A New Paradigm Bridging Foundation Models and Lifelong Agentic Systems  [paper] ★2477 | 2025-08-10 | idea-only | impact 7 novelty 5
- URL: https://arxiv.org/abs/2508.07407
- Evolves: prompts, context-memory, skills-tools, workflow-topology, model-weights, tasks-curriculum
- Mechanism: This is a survey, so the 'mechanism' is an abstraction, not a runnable loop. The paper's Figure 1 (verified from the repo asset) defines a 4-node cycle: (1) System Inputs I — task-level {T, D_train, D_test} or instance-level {x, y, C (context)}; (2) Agent System A — single-agent (prompt, tools, memory) or multi-agent (agents, communication, topology); (3) Environment — scenarios (coding, legal, research, medicine) that return Proxy Metrics: accuracy/F1/success rate or LLM-based evaluators; (4) Optimiser P = Search Space S (prompt template, tool selection, LLM parameters, architecture) x Optimi…
- Fitness/acceptance: In the abstraction, the Environment emits proxy metrics (accuracy, F1, success rate) computed on D_train/D_test at task level, or on a single (x, y) at instance level; where no ground truth exists, LLM-as-a-Judge / Agent-as-a-Judge provide the score. The survey's Evaluation section is a catalogue of static benchmarks (ToolBench, WebArena, SWE-bench, GAIA, OSWorld, AgentBench, MLGym) plus judge par…
- Key insight: Every self-evolving agent system reduces to one cycle — Inputs -> Agent System -> Environment (proxy metric) -> Optimiser -> mutate Agent System — where 'what evolves' is just the chosen Search Space S (prompt / tools / memory / weights / topology) and 'how' is the algorithm H (heuristic / text-gradient / MCTS / RL); a harness should therefore make S and H pluggable and keep the evaluator separate from both.
- Open gaps: No formalisation of the Optimiser->Inputs 'Refine' edge: task/curriculum self-generation is drawn in the figure but never taxonomised or given an operator; a fr | Evaluation of evolution itself is absent: no metrics for per-round improvement, regression rate, catastrophic forgetting, or held-out cross-task transfer of evo | 'Endure' and 'Excel' laws have no enforcement mechanism: no versioned artifact store, rollback, canary/shadow evaluation, sandboxing of self-modifications, or h | Evaluator is treated as a fixed environment output; no coverage of evaluator co-evolution, judge robustness, or defenses against the agent gaming an LLM-as-judg | Single-loop abstraction ignores multiple loops at different timescales (instance-level test-time refinement vs task-level offline optimisation vs weight updates

### Harness Engineering for Self-Improvement (Lil'Log)  [blog] ★- | 2026-07-04 | idea-only | impact 7 novelty 5
- URL: https://lilianweng.github.io/posts/2026-07-04-harness/
- Evolves: prompts, context-memory, skills-tools, workflow-topology, agent-source-code, model-weights, tasks-curriculum, harness-infrastructure, other
- Mechanism: The essay is a survey/position piece; it does not run a loop itself. It defines a harness as 'the system surrounding a base model that orchestrates execution and decides how the model thinks and plans, calls tools and acts, perceives and manages context, stores artifacts, and evaluates results', and argues near-term RSI will start by optimizing this layer, not weights. It organizes the field on an 'optimization ladder' of what is mutated: instruction prompts -> structured context (ACE playbook bullets; MCE bi-level skill/context) -> workflow graphs (ADAS archive + meta-agent; AFlow MCTS over L…
- Fitness/acceptance: Signal is always an external, verifier-grounded task score: Terminal-Bench-2 / SWE-bench Verified / Polyglot pass rates, AFlow/ADAS task accuracy, STOP's meta-utility (mean utility of the improver across a task distribution), MCE's train/val split (inner loop on train, outer on val). Anti-drift measures she highlights from cited work: Self-Harness accepts an edit only if there is no regression on…
- Key insight: Because a harness is code, harness design becomes an executable search space that a coding agent can explore with the same tools human engineers use; the transferable recipe is: expose every editable component as a file, mine failures into component-attributed patterns, propose bounded edits paired with falsifiable predictions, and accept only on zero regression across held-in and held-out splits, with the evaluator and permission layer kept read-only outside the loop.
- Open gaps: No reference implementation of the full ladder: ACE, MCE, Meta-Harness, Self-Harness, AHE and DGM each use bespoke file layouts and loops; a framework could def | Evaluator is fixed by fiat: the essay says the verifier must sit outside the loop but offers no mechanism to harden or extend evaluators for fuzzy domains (rese | No standard generalization gate: only AHE tests frozen-harness transfer to a second benchmark; acceptance criteria could require cross-benchmark or cross-task h | Harness edits are not model-conditioned despite non-monotonic harness-benefit: a framework could measure harness-updating and harness-benefit separately per bas | Rejected edits are logged (Self-Harness) but never used: no system feeds the negative-result archive back into proposal conditioning to avoid repeated dead ends

### ASI-Arch (AlphaGo Moment for Model Architecture Discovery)  [paper] ★1187 | 2025-07-24 | research-prototype | impact 6 novelty 6
- URL: https://arxiv.org/abs/2507.18074
- Evolves: other, context-memory
- Mechanism: The evolving artifact is a single PyTorch file (a DeltaNet-class linear-attention layer), not the agents. One experiment (pipeline.py) = sample -> evolve -> evaluate -> analyse -> update:

1. SAMPLE: candidate pool = top-50 archive entries by fitness. Parent drawn uniformly from ranks 1-10 (its code overwrites SOURCE_FILE); 4 references from ranks 11-50. Each entry's motivation/analysis/cognition are re-summarised by a low-temp LLM every cycle (never stored) to keep context non-static.
2. EVOLVE (Researcher, o3 + GPT-4.1): planner reads code, writes modified layer + motivation in one agent (to…
- Fitness/acceptance: Signal = composite fitness per architecture: sigmoid-capped final-train-loss delta and sigmoid-capped average-benchmark delta vs a fixed DeltaNet baseline (only ±10% window counts), plus an LLM-as-judge score on a rubric anchored to DeltaNet=5/GatedDeltaNet=10. Selection is elitist: only top-50 pool members are ever parents. Anti-hacking measures: sigmoid capping so no single metric dominates; dis…
- Key insight: Treat the archive's post-mortem analysis as a first-class evolvable memory: each child is analysed against its parent, grandparent and strongest siblings (a free pseudo-ablation), the identified shortcoming is used to retrieve distilled literature "cognitions", and both are stored on the node and fed to the next proposal — their provenance study claims the best designs lean more on this accumulated experience (44.8%) than the population average, i.e., evolving what the system knows about the search space matters as much as mutating the artifact. Secondary: sigmoid-capped deltas plus an LLM judge blunt single-metric reward hacking, and a cheap explore stage (20M) feeding an expensive verify stage (340M) is what made 1,773 trials affordable.
- Open gaps: Fitness is single-seed, 500-sample-per-task at 20M params with no variance estimate or held-out task suite; only ~8% of 20M winners survived the 340M re-check,  | The search procedure itself is frozen: planner prompts, judge rubric, dedup rules, and sampling policy never change in response to discovery yield — no meta-lev | One-third of fitness is an uncalibrated LLM judge whose largest weight (45% complexity/efficiency) is inferred from code and never measured; no human-rating cal | Failed or rejected experiments (evolution/eval failures) are not stored in the archive, so the memory has survivorship bias and the system cannot learn 'what no | Elitist top-50 scalar pool with no quality-diversity/niching (MAP-Elites style), so lineages collapsed onto gating/routing variants of one DeltaNet class; only 

### Self-Harness: Harnesses That Improve Themselves  [paper] ★101 | 2026-06-08 | research-prototype | impact 6 novelty 6
- URL: https://arxiv.org/abs/2606.09498
- Evolves: prompts, skills-tools, workflow-topology, harness-infrastructure
- Mechanism: Model weights and evaluator are frozen; only the harness h_t (a Python module built on LangChain Deep Agents, run under Harbor for Terminal-Bench) changes. Per round: (1) Weakness Mining: every failed train-split trace is normalized into steps/stages and an LLM (same model) labels each stage with terminal_cause / criticality / agent_mechanism; failures are clustered by exact signature tuple, ranked (root_cause first, then cluster size), and rendered into a markdown 'diagnosis brief' that also lists all passing cases as regression tests to preserve. (2) Harness Proposal: the same model is promp…
- Fitness/acceptance: Signal is benchmark pass rate (verifier-judged: Terminal-Bench-2.0 tests, SWE-bench Verified tests, AppWorld) on a train split plus a 'held-out' split, averaged over 2 repeats. Acceptance rule (acceptance/scripts/run_acceptance_gate.py): 'no split drops and at least one split improves' — a pure non-regression gate with no statistical test. Passing train cases are explicitly passed to the proposer…
- Key insight: Constrain self-modification to a small menu of typed, AST-replaceable harness hooks (prompt slots, subagent policy, skills, middleware, tools, permissions), force each candidate to change exactly one hook tied to a specific mined failure cluster, and admit it only through a train+held-out non-regression gate — this makes same-model self-improvement of the harness tractable and auditable without any stronger external optimizer.
- Open gaps: No truly held-out test set: the 'heldout' split participates in the acceptance gate, so a framework should add a third, never-consulted split or cross-validatio | Acceptance is 'any positive delta over 2 repeats' with no significance/noise model; needs sequential testing, more repeats, or bootstrap CIs before promoting an | Linear single-lineage search (one active branch, merged winners); no population/archive, no crossover across branches, no revisiting rejected-but-promising edit | No cross-model or cross-benchmark transfer: harnesses are re-derived per model x benchmark; no experiment applying Qwen's harness to GLM-5 or TB2 edits to SWE-b | Editable surface is a fixed hand-written hook menu inside one Deep Agents file; the loop cannot invent new hook types, tools with real code, or change control f

### Diving into Reliable Self-Evolving Agents: A Survey (Awesome-Reliable-Self-Evolving-Agents)  [paper] ★196 | 2026-08-10 | idea-only | impact 5 novelty 6.5
- URL: https://github.com/wkqdzkd/Awesome-Reliable-Self-Evolving-Agents
- Evolves: prompts, context-memory, skills-tools, workflow-topology, agent-source-code, model-weights, tasks-curriculum, reward-evaluator, harness-infrastructure
- Mechanism: This is a survey, not a system; its contribution is a formal frame for the self-evolution loop. Retained agent state is X_k = (theta_k, sigma_k, U_k, C_k): trainable weights, scaffold (prompts/tools/memory/workflows/harness), improver (procedure that proposes/selects/commits/rolls back updates), and internal criterion (rewards, rubrics, comparison rules). One round: (1) Exec under budget b_k yields trajectory, output y_k, experience e_k (may write runtime memory sigma_rt directly); (2) the current improver U_k Proposes candidate states from e_k and history H_k (H_k stored outside the update bo…
- Fitness/acceptance: Prescriptive only (survey runs no experiments). Per-level evidence the paper demands: L0 execution/source-grounded checks or human adjudication; L1 fresh post-update transfer, retention and interference tests plus data-admission checks; L2 matched-budget (same model, tools, tokens, stopping rule) end-to-end comparison vs incumbent on fresh tasks the edit did not select, component ablations, proven…
- Key insight: Classify a self-evolving agent by the deepest component its updates can rewrite (output, weights, scaffold, improver, criterion), then keep the external target, evidence source, and accept/reject/escalate gate outside that update boundary: no update should control the only evidence used for its own acceptance, and any evidence the proposer has already seen is development evidence, not audit evidence.
- Open gaps: No reference implementation of the audit loop (versioned external target, evidence source S, gate G with accept/reject/escalate, promotion record) — a harness f | No benchmark tests retained L3/L4 changes across multiple generations (does a descendant improver produce better later descendants on fresh task families under  | Holdout erosion is described but not operationalized: no access-budget ledger, query logging, or rotating fresh anchor sets for audit evidence | Matched-budget comparison (tokens, tool calls, wall-clock, stopping rule) is prescribed but no standardized cost-normalized fitness or reporting format exists | No tooling verifies that a retained scaffold/harness edit is actually active on later tasks (harness-updating vs harness-benefit decoupling) — need activation/p

### Awesome-Self-Evolving-Agents (companion list to "A Comprehensive Survey of Self-Evolving AI Agents", arXiv 2508.07407)  [repo] ★2477 | 2025-05-08 | usable | impact 7 novelty 4
- URL: https://github.com/ANative-Lab/Awesome-Self-Evolving-Agents
- Evolves: prompts, context-memory, skills-tools, workflow-topology, agent-source-code, model-weights, tasks-curriculum, reward-evaluator
- Mechanism: This repo has NO loop of its own: it is a README-only curated list (README.md + LICENSE + 3 figures, zero code) that indexes ~250 papers under the survey's taxonomy. The mechanism it codifies is the survey's abstract loop: four components — System Inputs I (task-level datasets vs instance-level single queries), Agent System A (LLM, prompts, memory, tools, and for MAS the topology/workflow), Environment E (benchmark or real-world feedback), and Optimiser P — where P is characterised by a search space (which parts of A are configurable) plus a search algorithm, and each iteration maps (I, A, fee…
- Fitness/acceptance: None for the repo itself (its only "signal" is community PRs adding entries). The survey's Section 4 catalogues evaluation signals used across the field: benchmark scores (SWE-bench, WebArena, GAIA, AgentBench, OSWorld, ToolBench-style suites), LLM-as-a-Judge and Agent-as-a-Judge for tasks without ground truth, and a safety/robustness benchmark set (AgentHarm, RedCode, R-Judge, AGrail, AutoDAN-Tur…
- Key insight: Every self-evolution method is the same abstract loop — an Optimiser with a declared search space over agent components (prompt, memory, tools, topology, weights, source code) that consumes environment feedback and emits a new agent — so a framework should treat "what is mutable" as a pluggable dimension, and must explicitly enforce the survey's Endure (safety) and Excel (no performance regression) laws as acceptance gates, not afterthoughts.
- Open gaps: No reference implementation of the four-component (Inputs/Agent/Environment/Optimiser) loop — a framework could ship the abstract interface so prompt, memory, t | Endure/Excel laws are stated but never operationalised: no concrete regression-gate protocol (held-out eval, safety suite re-run, automatic rollback) that an op | Taxonomy has no axis for the fitness signal (ground-truth benchmark vs LLM-judge vs execution tests vs human) or for how each method resists reward hacking — a  | No treatment of cross-task/cross-domain transfer of evolved artifacts (skills, prompts, memories) — most listed methods optimise per-benchmark and are re-run fr | Harness-level evolution (agent scaffolding, tool schemas, permission/sandbox config, hooks) is absent from the taxonomy — pending PRs (Harness-Aware Training, R

### Self-Improvements in Modern Agentic Systems: A Survey  [paper] ★471 | 2026-07-14 | idea-only | impact 6 novelty 5
- URL: https://arxiv.org/abs/2607.13104
- Evolves: prompts, context-memory, skills-tools, workflow-topology, agent-source-code, model-weights, tasks-curriculum, reward-evaluator, harness-infrastructure
- Mechanism: This is a survey, so the 'loop' is a formalism rather than a running system. Agent configuration A_t = (theta_t, Sigma_t): theta = frozen-or-trainable FM weights; Sigma = (p, m, T, g) = prompts, memory + read/write policy, tools + invocation schemas, control logic (routing/scheduling/safety). Transient execution state X_t (KV cache, dialogue history, working memory) is explicitly excluded — only durable commits to theta or Sigma count as self-improvement. Self-improvement is one self-induced update operator: A_{t+1} = U(A_{1:t}, E(pi_{theta,Sigma}; Sigma_t, C_t)), where E is the agent executin…
- Fitness/acceptance: Survey prescribes rather than runs an eval loop. Recommended protocol: track trajectory m_t = E[Phi(x, tau)] over iterations under a cumulative budget b_t <= B_max on a held-out D_eval (hidden or post-cutoff tasks), with explicit checkpoints, acceptance criteria and early stopping. Phi is either metric-based (executable unit tests, deterministic) or judge-based (LLM/Agent-as-a-Judge with rubric ka…
- Key insight: Treat the agent as (frozen weights, serializable scaffold Sigma=(prompts, memory, tools, control logic)) and treat every form of self-improvement — from DPO on weights to a coding agent patching its own harness — as one verifier-gated, self-induced update operator whose signal comes from the agent's own execution; do noisy, reversible adaptation in the scaffold fast loop and only consolidate into weights once behavior is proven stable, and keep the critic outside the agent's unconstrained control.
- Open gaps: No reference implementation of Sigma=(p,m,T,g) as typed, versioned substrates with a single generic gated commit/rollback API — a framework could ship this dire | Joint theta+Sigma optimization with cross-substrate credit assignment (decide per failure whether to edit a prompt, rewrite a tool wrapper, write memory, or fin | Verifier gate V is only specified for full-scaffold patches; no acceptance criteria or regression checks are defined for memory writes, prompt edits, or tool ad | No concrete protocol for safely evolving the critic/evaluator beyond 'additive test generation + human audit' — no mechanism to detect proposer/acceptor collusi | The recommended evaluation protocol (trajectory under budget, held-out transfer, regression rate, cost breakdown, quantified human input) exists only as a check

### Recursive Self-Improvement in AI: From Bounded Self-Refinement to Autonomous Research Loops  [paper] ★- | 2026-07 (a | idea-only | impact 5 novelty 6
- URL: https://arxiv.org/abs/2607.07663
- Evolves: prompts, context-memory, skills-tools, workflow-topology, agent-source-code, model-weights, tasks-curriculum, reward-evaluator, harness-infrastructure, other
- Mechanism: This is a survey, not a system; it has no loop of its own. Its contribution is a canonical description of every self-improvement loop as: (1) generate candidates, (2) score them with an evaluator, (3) select/apply the winner into a substrate, (4) persist or discard. Axis 1 = which substrate absorbs the improvement: deployment-time (frozen-weight output refinement -> test-time weight updates -> harness/skill/memory evolution, ordered by persistence: evaporates per episode < lasts a session < accumulates indefinitely), training-time (self-reward RL, STaR-style CoT self-training, on-policy self-d…
- Fitness/acceptance: The survey's own 'signal' is bibliographic: arXiv harvest across seven query threads enriched with OpenAlex metadata, then rule-based classification (theme defaults + title/abstract keyword rules moved 89 papers; 3 manual overrides), all by a single annotator with per-paper assignments released for audit -- no inter-annotator agreement reported. For the systems it surveys, the paper argues the imp…
- Key insight: Self-improvement is only as real as its verification signal: demonstrated gains track a strict hierarchy (formal verifier > execution feedback > learned judge > intrinsic self-assessment), and the risk of a loop is set by the persistence of what it modifies (outputs evaporate < weights roll back < harness/skills/memory accumulate and propagate) -- so a framework should pick the highest-rung signal available per task, keep a fixed external anchor even when co-evolving its evaluator, and constrain the self-modification surface to artifacts the chosen signal can actually audit, diff, and roll back.
- Open gaps: No 'exchange rate of grounding': nobody has measured the minimum fraction of exogenous (human/formal/execution) signal per iteration that keeps a loop improving | Trustworthy accumulation: persistent self-modifications (skills, memory, harness code, experience graphs) have no verification/rollback story comparable to weig | Frame-revision detection has neither benchmark nor metric: build a suite of deliberately stale or misspecified objectives scored on whether the loop notices (pr | Evaluator co-evolution (Red Queen, self-trained verifiers, self-evolved rubrics) is unproven against the self-confirming loop; frameworks that evolve their judg | Stability engineering is per-paradigm folklore: no unified detector for rise-and-collapse, diversity collapse, or safety drift; a harness could track output ent

### Evolver (EvoMap)  [repo] ★9055 | 2026-02-01 | usable | impact 5 novelty 5
- URL: https://github.com/EvoMap/evolver
- Evolves: prompts, context-memory, skills-tools, agent-source-code, tasks-curriculum, other
- Mechanism: Evolver is a Node CLI that runs beside a coding agent (OpenClaw, Claude Code, Cursor, Codex hooks) in a git repo. Per cycle (verified in readable v1.28 source; current core is obfuscated): (1) Regex-scan session transcripts/logs/MEMORY.md into string "signals" (log_error, errsig:<hash>, perf_bottleneck, user_feature_request, stagnation flags). (2) Select a Gene from genes.json by counting substring/regex hits of gene.signals_match against signals. A memory-graph JSONL of signal->gene outcomes (30-day half-life) supplies a preferred gene and bans genes with expected success <0.18 after 2+ attem…
- Fitness/acceptance: Acceptance is binary pass/fail of gene-declared validation commands plus blast-radius/forbidden-path constraint checks and a canary import; the recorded score is a constant (0.85/0.2), not a task metric. Anti-drift: circuit breaker forces 'innovate' after 3 consecutive failed repairs; signals seen in 3+ of last 8 events are suppressed; genes banned via decayed outcome ledger; history block in prom…
- Key insight: Encode accumulated agent experience as compact structured 'genes' (trigger signals -> strategy steps -> AVOID cues -> validation hooks, ~230 tokens) rather than long skill docs, and make every self-modification a git-scoped, validation-gated, rollback-able event logged in a signal->gene outcome ledger so selection can ban low-yield paths and share validated capsules across nodes.
- Open gaps: Fitness is a constant 0.85/0.2 derived from self-declared validation commands; no task-level or held-out benchmark score enters gene selection | Proposer and verifier are the same agent: genes carry their own validation commands (e.g. 'node --version'), so a gene-independent, tamper-proof evaluator is mi | Gene selection is substring/regex hit counting over regex-extracted log signals; no semantic retrieval or embedding match of situation to strategy | Auto-created genes get a boilerplate strategy and auto-distilled strategies are truncated excerpts of the agent's own turns; no quality gate or LLM refinement t | Closed-loop transfer is unverified by maintainers: v2 inject session-start drops strategy text (issue #624), so learned fixes do not reach the next session

### Awesome-Self-Evolving-Agents (XMUDeepLIT) / "A Systematic Survey of Self-Evolving Agents: From Model-Centric to Environment-Driven Co-Evolution"  [other] ★426 | 2026-02-09 | usable | impact 5 novelty 4
- URL: https://github.com/XMUDeepLIT/Awesome-Self-Evolving-Agents
- Evolves: prompts, context-memory, skills-tools, workflow-topology, agent-source-code, model-weights, tasks-curriculum, reward-evaluator, harness-infrastructure
- Mechanism: This is a curated reading list plus companion survey, not a runnable system: nothing in the repo executes a loop. What it contributes is a taxonomy of loops. The survey defines an agent as a tuple <Phi (policy/weights), M (memory), T (tools), I (interaction interface; perception P was folded into I after issue #2)> and defines self-evolution by two properties: "strong autonomy with minimal human supervision" and "active exploration through interaction" (maintainer reply, issue #4). Three evolvable substrates: (1) Model-centric: inference-time (self-consistency, Self-Refine, ToT) or training-ti…
- Fitness/acceptance: None intrinsic -- the repo is a document. For the surveyed literature the signal varies by branch: verifiable rewards/unit tests and self-play win-rates for weight evolution; task success or self-judgment for experience/skill evolution; held-out downstream tasks only in the three Sept-2026 "self-evolution evaluation" benchmarks (Aspire, S3Gym, HarnessDev) and in AgentDescent's Beta-posterior accep…
- Key insight: Treat the agent as a tuple (weights, memory/experience, tools/skills, topology, environment/curriculum) and pick the cheapest substrate to mutate: the field is moving from mutating weights (needs GPU RL and verifiable rewards) to mutating environment-facing state (skills, context, harness) and finally the environment itself (co-evolution), and the only honest measure of a self-evolution loop is improvement on hidden held-out tasks after the loop runs, not on the tasks the loop trained on.
- Open gaps: No surveyed system evolves multiple substrates (prompts, skills, memory, tools, topology, curriculum) under one shared acceptance gate; each paper mutates one s | Regression prevention is not even a taxonomy category: versioned artifacts, rollback, canary deployment and a frozen regression suite appear only in community-a | Fitness for experience/skill evolution (ACE, ReasoningBank, Dynamic Cheatsheet, Memento) is in-distribution task success or self-judgment; held-out/hidden-task  | Cross-task and cross-domain transfer of learned skills is unmeasured except AgentKB/CoPS; a skill-library transfer benchmark and skill-pruning policy are missin | Credit assignment for non-parametric evolution is untracked: no method records which memory/skill entry caused which improvement, so libraries grow monotonicall

### Self-Evolving Coding Agents (survey)  [paper] ★139 | 2026-08-04 | idea-only | impact 4 novelty 4
- URL: https://arxiv.org/abs/2608.03392
- Evolves: agent-source-code, context-memory, skills-tools, model-weights, workflow-topology, prompts, tasks-curriculum, reward-evaluator, harness-infrastructure
- Mechanism: This is a survey, not a system; it contributes no loop of its own but a three-axis frame for describing loops in coding agents. Axis 1, the object of evolution (RQ1): (a) agent framework / scaffold rewriting and archive-based variant search (SICA, SIFT, STOP, DGM, Mendel GM, Huxley GM); (b) memory: experience banks, subtask-aligned memory, repository memory from commit history, vulnerability-repair experience (SWE-Exp, EvoCoder, Subtask Memory, RepoMemory, EvoRepair, SAGE); (c) skills and tools distilled from trajectories or learned per repository, plus on-the-fly tool synthesis (CODESKILL, gs…
- Fitness/acceptance: The survey runs no experiments and reports no numbers of its own. It catalogs the signals used by surveyed systems: outcome evidence (SWE-bench/Verified/Lite/Pro resolve rate, HumanEval/MBPP/APPS/CodeContests/LiveCodeBench pass rate, cost/runtime as in SICA, archive fitness as in DGM), environmental feedback (unit tests, compiler diagnostics, CI logs, sandboxed execution, adversarially generated t…
- Key insight: Software engineering is the natural substrate for agent self-evolution because its feedback is executable and repeatable, but a loop only counts as self-evolution when the signal closes around the agent's own attempts; the same executable signals that make evolution cheap also propagate errors into persistent state (memory, skills, scaffold, weights), so validation, rollback and held-out transfer must be first-class parts of the loop, not afterthoughts.
- Open gaps: No system in the survey evolves more than one object at a time in a coordinated way (e.g. joint scaffold + skill + memory updates with shared attribution); a fr | Rollback and verification of self-modifications are only called for, never specified: no surveyed system offers versioned scaffold state, regression gates, or a | Fitness is almost always a single public benchmark (SWE-bench variants, HumanEval); no protocol for held-out repositories, contamination-aware task generation,  | Memory and skill stores have no staleness, redundancy, or contamination controls; nothing surveyed retires memories/skills when repository state changes or when | Evidence reliability is untreated: no surveyed loop weights signals by test coverage, flakiness, or verifier calibration, so misleading feedback propagates into


## Remaining index (not deep-read), top 120 by priority

- [repo] Darwin Gödel Machine (DGM) — Sakana AI / UBC (Zhang, Hu, Lu, Lange, Clune): coding agent that rewrites its own code, keeps an open-ended archive of variants, and empiric (https://github.com/jennyzzt/dgm, ★2311, 2025-05)
- [repo] Huxley-Gödel Machine (HGM) — Wang, Piękos et al. (KAUST/Schmidhuber group): guides the DGM self-modification tree with clade metaproductivity (CMP) plus Thompson-samplin (https://github.com/metauto-ai/HGM, ★431, 2025-10)
- [blog] Anthropic Institute: When AI builds itself (recursive self-improvement) — Anthropic's evidence report on Claude running AI research end-to-end: >80% of merged production code by Claude, ~52x code-optimization speed (https://www.anthropic.com/institute/recursive-self-improvement, 2026-05)
- [paper] HarnessDev: Can LLMs Create and Evolve Their Own Agent Harness? — Benchmark (Wu, Zhang + 17 authors) whose unit of evaluation is runnable infrastructure: agents build a harness from a minimal seed (Creation (https://arxiv.org/abs/2609.01437, 2026-09)
- [repo] Skill Self-Play (Skill-SP, Qwen/Alibaba) — Proposer, solver, and a dynamic skill controller co-evolve: skill-routed task generation, validity checks, frontier-based curriculum, and fe (https://github.com/Qwen-Applications/skill-self-play, ★144, 2026-07)
- [repo] Arbor — Generalist autonomous research agent that grows a hypothesis tree, runs real experiments and keeps only held-out gains; claims 2.5x over Cla (https://github.com/RUC-NLPIR/Arbor, ★1063, 2026-05)
- [paper] Harnessing Agentic Evolution (AEvo) — Treats the evolution process itself as an environment: a meta-agent reads accumulated candidates, feedback, traces, failures and cost histor (https://arxiv.org/abs/2605.13821, 2026-05)
- [repo] A-EVO-Lab/a-evolve — 'PyTorch for agentic evolution': ae.Evolver(agent, benchmark).run(cycles=10) evolves any agent with any evolution algorithm; #1 MCP-Atlas, ~ (https://github.com/A-EVO-Lab/a-evolve, ★779, 2026-02)
- [paper] ShinkaEvolve (Sakana AI) — Lange, Imajuku, Cetin: open-ended, sample-efficient LLM program evolution with exploration/exploitation parent sampling, novelty rejection s (https://arxiv.org/abs/2509.19349, ★1380, 2025-09)
- [repo] agentdescent — 'Gradient descent, but the parameters are agents': parallel asynchronous framework that evolves skills, prompts and harnesses; code diffs ac (https://github.com/Birfy/agentdescent, ★217, 2026-07)
- [repo] darwin-skill — Autoresearch-style evaluate -> improve -> test -> keep/revert loop that makes any Claude Code skill self-evolve; 5.9k stars, bilingual. (https://github.com/alchaincyf/darwin-skill, ★5943, 2026-04)
- [repo] autocontext — 'Recursive self-improving harness': runs a task against evaluation, keeps useful lessons, discards dead ends, leaves traces/playbooks/datase (https://github.com/greyhaven-ai/autocontext, ★1292, 2026-02)
- [repo] MemSkill — Learning and evolving memory skills for self-evolving agents — the agent learns how to manage its own memory as evolvable skills. (https://github.com/ViktorAxelsen/MemSkill, ★572, 2026-01)
- [repo] Yunjue Agent — Fully reproducible, zero-start in-situ self-evolving agent system for open-ended tasks. (https://github.com/YunjueTech/Yunjue-Agent, ★533, 2026-01)
- [paper] Improving the matrix multiplication exponent with modern optimization and AlphaEvolve — DeepMind + Alman + Vassilevska Williams: reformulated laser-method optimization, ML-style optimizers, then AlphaEvolve refinement gives omeg (https://arxiv.org/abs/2608.16884, 2026-08)
- [paper] Mathematical exploration and discovery at scale (Georgiev, Gomez-Serrano, Tao, Wagner) — AlphaEvolve run on 67 problems across analysis, combinatorics, geometry, number theory; improved best-known on 23, matched on 36; released p (https://arxiv.org/abs/2511.02864, 2025-11)
- [paper] EvoX: Meta-Evolution for Automated Discovery (UC Berkeley / Sky) — Jointly evolves candidate solutions AND the search strategy (selection/variation operators) based on progress; beats AlphaEvolve, OpenEvolve (https://arxiv.org/abs/2602.23413, 2026-02)
- [repo] Memento: Fine-tuning LLM Agents without Fine-tuning LLMs (orig. AgentFly) — Memory-augmented MDP with case-based reasoning: episodic case bank + neural case-selection policy updated from environment feedback; 87.88%  (https://github.com/Memento-Teams/Memento, ★2571, 2025-08)
- [paper] The AI Scientist-v2 (Sakana AI) — Template-free end-to-end research agent using progressive agentic tree search with an experiment manager and VLM figure critique; one fully  (https://arxiv.org/abs/2504.08066, ★7126, 2025-04)
- [paper] Towards End-to-End Automation of AI Research (Nature 2026) — Nature 651, 914-919 (2026): The AI Scientist creates ideas, writes code, runs experiments, writes the manuscript and reviews itself; two mod (https://arxiv.org/abs/2606.15497, 2026-06)
- [repo] SICA: A Self-Improving Coding Agent — Robeyns, Szummer, Aitchison (Bristol): eliminates the meta-agent/target-agent split, the best archived agent edits its own codebase to impro (https://github.com/MaximeRobeyns/self_improving_coding_agent, ★394, 2025-04)
- [repo] Automated Design of Agentic Systems (ADAS / Meta Agent Search) — Hu, Lu, Clune (UBC/Vector): a meta agent programs ever-better agents in code using a growing archive of discoveries; discovered agents beat  (https://github.com/ShengranHu/ADAS, ★1637, 2024-08)
- [repo] AutoAgent (thirdlayer) - autonomous harness engineering — Karpathy-autoresearch pattern applied to agent harnesses: a meta-agent driven by program.md edits system prompt, tools, config and orchestra (https://github.com/thirdlayerinc/autoagent, ★4570, 2026-04)
- [repo] TTRL: Test-Time Reinforcement Learning — RL on unlabeled test data using majority-vote as the reward signal; NeurIPS 2025. (https://github.com/PRIME-RL/TTRL, ★1123, 2025-04)
- [repo] SPIRAL — Self-play on two-player zero-sum language games (Kuhn Poker, TicTacToe, etc.) with role-conditioned advantage estimation; reasoning transfer (https://github.com/spiral-rl/spiral, ★205, 2025-06)
- [paper] SPICE: Self-Play In Corpus Environments (Meta) — Challenger mines a document corpus to create reasoning tasks; Reasoner solves them; corpus grounding supplies an inexhaustible external sign (https://arxiv.org/abs/2510.24684, 2025-10)
- [paper] Self-Challenging Language Model Agents (Meta FAIR) — Agent plays challenger (creates Code-as-Task problems with verification) and executor; supports both distillation and pure self-improvement  (https://arxiv.org/abs/2506.01716, 2025-06)
- [repo] Microsoft R&D-Agent — Dual-agent Research/Development loop that iteratively proposes ideas from performance feedback and refines code from error feedback, with pa (https://github.com/microsoft/RD-Agent, ★14582, 2024-04)
- [repo] The AI Scientist (v1) - Sakana AI — First end-to-end automated ML research pipeline: idea generation, experiment execution, paper writing, automated review (~$15/paper). Paper  (https://github.com/SakanaAI/AI-Scientist, ★14529, 2024-08)
- [repo] MLEvolve — Self-evolving multi-agent framework for end-to-end ML algorithm discovery: progressive Monte-Carlo graph search + retrospective global memor (https://github.com/InternScience/MLEvolve, ★436, 2026-02)
- [repo] Hyra (Tencent Hunyuan Research Agent) results — Artifacts from Tencent's Hyra research agent: new bounds on open math problems (e.g., Beurling-Ahlfors coefficient 1.575 -> 1.523958, commut (https://github.com/Tencent-Hunyuan/Hyra-results, ★154, 2026-07)
- [repo] SkillRL: Evolving Agents via Recursive Skill-Augmented RL — Distills successful trajectories into strategic patterns and failures into lessons in a hierarchical SkillBank (general + task-specific) tha (https://github.com/aiming-lab/SkillRL, ★970, 2026-02)
- [paper] Evo-Memory: Benchmarking LLM Agent Test-time Learning with Self-Evolving Memory (Google DeepMind) — Benchmark over 10 multi-turn goal-oriented and reasoning/QA datasets for memory that evolves across a task stream, plus ReMem (reason/act/me (https://arxiv.org/abs/2511.20857, 2025-11)
- [repo] FunSearch (Google DeepMind) — Pair a pretrained LLM with an automated evaluator in an island-based genetic loop over program skeletons; discovered new cap-set constructio (https://github.com/google-deepmind/funsearch, ★1117, 2023-12)
- [repo] Mathematical exploration and discovery at scale (Georgiev, Gómez-Serrano, Tao, Wagner) + AlphaEvolve problem repository — 67 mathematical problems (analysis, combinatorics, geometry, number theory) attacked with AlphaEvolve, with prompts, verifiers, seed program (https://github.com/google-deepmind/alphaevolve_repository_of_problems, ★235, 2025-11)
- [repo] AlphaEvolve on Google Cloud (client library + examples) — Official client-side Python library and runnable examples (circle packing to GPU training on GKE) for the AlphaEvolve service in Gemini Ente (https://github.com/Google-Cloud-AI/alphaevolve-on-googlecloud, ★110, 2026-07)
- [repo] ThetaEvolve: Test-time Learning on Open Problems — Simplifies AlphaEvolve to a single LLM + large program DB, then RL-trains that LLM at test time on the evolution rewards; an 8B model (Disti (https://github.com/ypwang61/ThetaEvolve, ★177, 2025-11)
- [paper] What Do Evolutionary Coding Agents Evolve? (EvoTrace + EvoReplay, ZIB) — Dataset of 121 full evolutionary runs across OpenEvolve, GEPA, EvoX, ShinkaEvolve on 16 tasks; finds ~30% of added lines are re-introduction (https://arxiv.org/abs/2605.20086, ★5, 2026-05)
- [paper] Group-Evolving Agents (GEA) — Weng, Antoniades, Nathani, Zhang, Pu, Xin Eric Wang (UCSB): treats a group of self-modifying agents as the evolutionary unit with explicit e (https://arxiv.org/abs/2602.04837, ★133, 2026-02)
- [repo] ReMe: Memory Management Kit for Agents (AgentScope / Alibaba Tongyi) — Local-first, self-evolving Markdown personal knowledge base that turns conversations/trajectories into refined experience for agents (Rememb (https://github.com/agentscope-ai/ReMe, ★3445, 2024-08)
- [repo] WebRL: Self-Evolving Online Curriculum RL (THUDM / Zhipu) — Trains open web agents (GLM-4/Llama) with a self-evolving curriculum that generates new tasks from failures plus an outcome reward model. (https://github.com/THUDM/WebRL, ★540, 2024-11)
- [paper] Training-Free GRPO (Tencent Youtu) — Replaces parameter updates with group-relative semantic advantages distilled into an evolving experience library used as a token prior; with (https://arxiv.org/abs/2510.08191, 2025-10)
- [paper] Rethinking the Evaluation of Harness Evolution for Agents — AI2 / UW (Wang, Hajishirzi, Tsvetkov, Dasigi, Xiao et al.): shows harness-evolution methods must be compared with task-level search baseline (https://arxiv.org/abs/2607.12227, ★34, 2026-07)
- [repo] Claude Autoresearch Skill — Claude Code skill implementing modify -> verify -> keep/discard -> repeat forever against any metric. (https://github.com/uditgoenka/autoresearch, ★6287, 2026-03)
- [repo] Search Self-Play (SSP, Alibaba Quark) — Adversarial proposer/solver self-play where both roles call a search engine in multi-turn reasoning; rule-based outcome rewards, per-role RL (https://github.com/Alibaba-Quark/SSP, ★107, 2025-10)
- [paper] AI4AI-Bench — Benchmark for recursive self-improvement: 10 frozen research repos across 10 training-algorithm families; agent gets 4h on one B300 to rewri (https://arxiv.org/abs/2608.20318, ★38, 2026-08)
- [repo] PenguinHarness — 'Harness for RSI': local multi-agent auto-dev platform where an agent runs the benchmark, finds lost points, ships version N+1 with snapshot (https://github.com/Prism-Shadow/penguin-harness, ★2097, 2026-07)
- [repo] MemOS — Self-evolving memory OS for LLM agents: persistent memory, hybrid retrieval, cross-task skill reuse; sibling paper MemRL (runtime RL on epis (https://github.com/MemTensor/MemOS, ★11284, 2025-07)
- [paper] ALMA: Learning to Continually Learn via Meta-learning Agentic Memory Designs — Meta-learns the agent memory design (what to store, how to retrieve/update) instead of hand-engineering it; learned designs beat SOTA human- (https://arxiv.org/abs/2602.07755, 2026-02)
- [repo] MemRL: Self-Evolving Agents via Runtime RL on Episodic Memory — Non-parametric self-evolution: two-phase retrieval over episodic memory whose utilities are updated by environmental feedback (runtime RL),  (https://github.com/MemTensor/MemRL, ★171, 2026-01)
- [repo] evo — Turns any codebase into an autoresearch loop: discovers what to measure, instruments the benchmark, runs tree search with parallel subagents (https://github.com/evo-hq/evo, ★1450, 2026-04)
- [repo] Mendel Gödel Machine (MGM) — Liu, Tresp, Ma et al. (LMU): adds reaction-norm mutation (edit agent from trajectories on many tasks at once) and cross-lineage hybridizatio (https://github.com/RealLcz/MGM, ★30, 2026-08)
- [repo] Group-Evolving Agents (GEA) — UCSB (William Wang group): treats a group of self-modifying agents as the evolutionary unit with explicit experience sharing instead of DGM' (https://github.com/UCSB-AI/GEA, ★133, 2026-02)
- [repo] AutoSaddler (Microsoft) — Frames harness improvement as offline learning over execution traces: failure-trace diagnosis, structured patches to prompts/tools/middlewar (https://github.com/microsoft/AutoSaddler, ★216, 2026-08)
- [other] OpenAI 'automated research intern' milestone — OpenAI says it hit its Sept-2026 target of an automated research intern that carries out multi-day well-defined research tasks; ~3.1 agent-w (https://www.engadget.com/2251859/openai-says-it-has-reached-its-goal-of-creating-an-automated-research-intern/, 2026-09)
- [repo] DeepScientist — Month-scale autonomous discovery system using Bayesian optimization over a hierarchical evaluator: ~5,000 ideas generated, ~1,100 experiment (https://github.com/ResearAI/DeepScientist, ★3323, 2025-09)
- [paper] AIRA2: Overcoming Bottlenecks in AI Research Agents (Meta) — Meta FAIR's second-gen AI research agent: async multi-GPU search, Hidden Consistent Evaluation, ReAct with dynamic action scoping; 81.5% mea (https://arxiv.org/abs/2603.26499, 2026-03)
- [paper] Towards Execution-Grounded Automated AI Research (Stanford) — Si, Yang, Choi, Candes, D. Yang, Hashimoto: an executor implements research ideas and runs GPU experiments; evolutionary search lifts post-t (https://arxiv.org/abs/2601.14525, 2026-01)
- [repo] EvoSkill (Sentient) — Toolkit that automatically discovers and synthesizes reusable agent skills from failed trajectories; proposes joint skill+prompt mutations,  (https://github.com/sentient-agi/EvoSkill, ★1180, 2026-03)
- [repo] Acontext — Agent Skills as a Memory Layer — Open-source skill-memory layer that automatically captures learnings from agent runs and stores them as editable, shareable agent skill file (https://github.com/memodb-io/Acontext, ★3686, 2025-07)
- [repo] tigerless-labs/autoharness — Self-learning skill layer for Claude Code: distills skills from real sessions, merges same-scenario skills, updates them in use, prunes unus (https://github.com/tigerless-labs/autoharness, ★3823, 2026-06)
- [repo] aiming-lab/MetaClaw — Meta-learn and evolve an OpenClaw-style agent from every conversation: skills mode + scheduled LoRA RL training (via Tinker) + cross-session (https://github.com/aiming-lab/MetaClaw, ★3495, 2026-03)
- [repo] EvoX: Meta-Evolution for Automated Discovery (SkyDiscover, UC Berkeley) — Two-level evolution: inner loop evolves candidate programs, outer loop evolves the search strategy itself when progress plateaus; beats Alph (https://github.com/skydiscover-ai/skydiscover, ★634, 2026-02)
- [paper] SICA: A Self-Improving Coding Agent — Maxime Robeyns et al. (Bristol): a coding agent that edits its own codebase (tools, sub-agents, prompts) in a benchmark-scored loop; reports (https://arxiv.org/abs/2504.15228, ★394, 2025-04)
- [paper] Mendel Gödel Machine (MGM) — Liu, Liu, Yan, Tresp, Ma (UESTC/LMU/MCML): self-modification conditioned on comparative evidence across the archive via three operators (clo (https://arxiv.org/abs/2608.07645, ★30, 2026-08)
- [paper] Agentic Harness Engineering (AHE) — Lin et al. (Fudan/Shanghai AI Lab): observability-driven automatic evolution of coding-agent harnesses (component-level file repr, evidence  (https://arxiv.org/abs/2604.25850, ★879, 2026-04)
- [paper] A Systematic Survey of Self-Evolving Agents: From Model-Centric to Environment-Driven Co-Evolution (Xiang et al., XMU) — 2026 survey with a three-part taxonomy: Model-Centric, Environment-Centric, and Model-Environment Co-Evolution; argues co-evolution with env (https://www.techrxiv.org/doi/10.36227/techrxiv.177203250.05832634, ★426, 2026-02)
- [repo] selfimproving-agent/awesome-Self-Improving-Agents (companion to Ren et al. survey) — 471-star, actively maintained list organized by FM-improvement vs scaffolding-improvement, including a 'Full Scaffolding' table of 30+ harne (https://github.com/selfimproving-agent/awesome-Self-Improving-Agents, ★471, 2025-12)
- [repo] SPADE: Self-Play in Adaptive Synthetic Executable Environments — One LLM is both Environment Designer (writes executable reset()/step() Python envs, grounded in a pretraining corpus + env memory) and Reaso (https://github.com/spade-rl/spade, ★99, 2026-08)
- [paper] Simple Baselines are Competitive with Code Evolution (Gideoni, Risi, Gal) — Two trivial baselines match or beat sophisticated code-evolution pipelines on math bounds, agent scaffold design and ML competitions; search (https://arxiv.org/abs/2602.16805, 2026-02)
- [paper] Autonomous Mathematical Discovery in an Open-World Multi-Agent Environment (The Station) — Multi-agent open-world research environment where agents from different model families pick problems, experiment and build a shared literatu (https://arxiv.org/abs/2608.23691, 2026-08)
- [repo] MemEvolve: Meta-Evolution of Agent Memory Systems (+ EvolveLab) — ICML 2026: jointly evolves the memory contents and the memory architecture itself (encode/store/retrieve/manage modules); EvolveLab unifies  (https://github.com/bingreeky/MemEvolve, ★266, 2025-12)
- [paper] Evo-Harness: Context-to-Harness Skill Compilation for Self-Evolving Agents — EMNLP 2026 Main: frozen LLM agents improve across sequential tasks by compiling noisy single-shot executions into reusable structured skill  (https://arxiv.org/abs/2608.15071, 2026-08)
- [paper] Agent-World: Scaling Real-World Environment Synthesis for Evolving General Agent Intelligence — Self-improving training arena that autonomously discovers environment-aligned tasks from thousands of real-world themes with controllable di (https://arxiv.org/abs/2604.18292, 2026-04)
- [repo] AgentGym-RL — Unified multi-turn RL framework for training LLM agents from scratch (no SFT) across web, search, games, embodied, science environments; Sca (https://github.com/WooooDyy/AgentGym-RL, ★864, 2025-09)
- [paper] Practice Makes Unsafe: Skill Misevolution in Self-Improving LLM Agents (SkillMisevo-Gym/Bench + SafeEvolve) — Gym + benchmark with 9 metrics for how unsafe practice turns into reusable skills; 21/21 evolved configs produced unsafe artifacts, carryove (https://arxiv.org/abs/2608.12851, ★6, 2026-08)
- [paper] EvoHarnessBench: Can Your Agents Keep Pace with an Evolving Harness? (Salesforce Research) — 17 multi-stage harness streams, 802 tasks, 520 tools, 42 skills, 62 agents; finds 'harness-induced forgetting' where adding capabilities deg (https://arxiv.org/abs/2609.04280, 2026-09)
- [paper] Anthropic: Natural emergent misalignment from reward hacking (From shortcuts to sabotage) — Models that learn to reward-hack coding RL environments generalize to sabotage of safety research code (12% via Claude Code) and alignment-f (https://www.anthropic.com/research/emergent-misalignment-reward-hacking, 2025-11)
- [blog] Anthropic: Automated researchers can reliably mitigate alignment failures — Claude run in a closed literature->method->train->test loop closes 26-96% of the safety gap across 10 alignment failure categories (85% on d (https://www.anthropic.com/research/automated-researchers-mitigate-alignment-failures, 2026-08)
- [blog] METR: Expenditure Horizon (NanoGPT speedrun optimization) — New metric = dollars at which an agent's optimization matches human returns; on nanoGPT speedrun humans spend ~$2,500 per 1% speedup; GPT-5/ (https://metr.org/blog/2026-07-21-expenditure-horizon/, 2026-07)
- [repo] Prime Intellect: Frontier Automated NanoGPT Speedrun — 153 autonomous multi-day runs of 18 frontier models on the modded-nanogpt track-3 optimizer speedrun (8xH200, frozen verify.py, 8-seed recor (https://github.com/PrimeIntellect-ai/frontier-automated-speedrun, ★18, 2026-08)
- [repo] RSI-Exam — aiming-lab: benchmark measuring recursive self-improvement on long-horizon executable research tasks. Aug 2026. (https://github.com/aiming-lab/RSI-Exam, ★101, 2026-08)
- [repo] Bilevel Autoresearch: Meta-Autoresearching Itself — Outer loop analyzes the inner autoresearch loop's trace and injects new search mechanisms as Python code at runtime, same LLM at both levels (https://github.com/EdwardOptimization/Bilevel-Autoresearch, ★181, 2026-03)
- [repo] ECC (Everything Claude Code) - agent harness performance optimization system — Massive Claude Code/Codex/Cursor harness layer (68 agents, 291 skills, hooks, memory) with continuous-learning v2: an observer daemon distil (https://github.com/affaan-m/ECC, ★256168, 2026-01)
- [paper] Socratic-Zero — Teacher, Solver, and Generator agents co-evolve from ~100 seed questions; Teacher targets Solver weaknesses, Generator distills the Teacher' (https://arxiv.org/abs/2509.24726, 2025-09)
- [paper] Self-Questioning Language Models (SQLM) — Asymmetric self-play from a single topic prompt: proposer rewarded for medium-difficulty questions, solver rewarded via majority vote (or un (https://arxiv.org/abs/2508.03682, 2025-08)
- [paper] Self-Play Only Evolves When Self-Synthetic Pipeline Ensures Learnable Information Gain — Analysis (KCL) showing self-play loops plateau when synthesized data stops adding learnable information; proposes asymmetric co-evolution, c (https://arxiv.org/abs/2603.02218, 2026-03)
- [repo] EvoMap AutoResearch — Open-source stateful, recoverable AI/ML research workflow from idea discovery through pilot experiments, independent multi-model review and  (https://github.com/EvoMap/AutoResearch, ★3373, 2026-08)
- [paper] Kosmos: An AI Scientist for Autonomous Discovery (Edison Scientific) — 12-hour runs cycling parallel data analysis, literature search and hypothesis generation over a structured world model; ~200 rollouts, 42,00 (https://arxiv.org/abs/2511.02824, ★582, 2025-11)
- [paper] SePO: Self-Evolving Prompt Agent for System Prompt Optimization — Self-referential design: a single prompt agent optimizes task agents' system prompts AND its own system prompt under open-ended evolutionary (https://arxiv.org/abs/2606.04465, ★4, 2026-06)
- [repo] FAPO: Fully Automated Prompt Optimization (cisco-foundation-ai) — Uses Claude Code as the autonomous optimizer over LangGraph pipelines: evaluate -> step-level failure attribution -> edit prompts, agent ski (https://github.com/cisco-foundation-ai/fully-automated-prompt-optimization, ★107, 2026-04)
- [blog] Building State-of-the-Art Enterprise Agents 90x Cheaper with Automated Prompt Optimization (Databricks) — GEPA-optimized gpt-oss-120b beats Claude Sonnet 4 / Opus 4.1 by ~3% at 20x/90x lower serving cost; also lifts Opus 4.1 and Sonnet 4 by 6-7%; (https://www.databricks.com/blog/building-state-art-enterprise-agents-90x-cheaper-automated-prompt-optimization, 2025-09)
- [paper] Promptbreeder: Self-Referential Self-Improvement via Prompt Evolution — DeepMind (Fernando et al.): evolutionary population of task-prompts whose mutation-prompts are themselves evolved by the LLM, i.e. self-refe (https://arxiv.org/abs/2309.16797, 2023-09)
- [other] ICLR 2026 Workshop on AI with Recursive Self-Improvement — Workshop on algorithmic foundations for reliable self-improving AI (agents rewriting codebases/prompts, continual fine-tuning pipelines, rob (https://iclr.cc/virtual/2026/workshop/10000796, 2026-04)
- [repo] Raven (EverMind) — Open-source self-improving agent harness (terminal-first, tracing, long-term memory, skills, evaluation, workflows); roadmap toward a 'Harne (https://github.com/EverMind-AI/Raven, ★3822, 2026-05)
- [repo] Kiro Crew (AWS) — Persistent dev workspace that is self-learning (corrections/failures become durable lessons) and self-evolving (repeated patterns become reu (https://github.com/kirodotdev/KiroCrew, ★3841, 2026-07)
- [repo] Meta Context Engineering via Agentic Skill Evolution (ICML 2026) — Bi-level framework co-evolving context-engineering skills and context artifacts; +18.4/+33.0 pts relative gain vs ACE offline/online across  (https://github.com/metaevo-ai/meta-context-engineering, ★166, 2026-01)
- [paper] SkillOS: Learning Skill Curation for Self-Evolving Agents (Google) — RL-trains a skill curator (composite rewards, grouped task streams) that manages an external skill repository so agents reuse experience ins (https://arxiv.org/abs/2605.06614, 2026-05)
- [repo] Agent Workflow Memory (AWM) — Induces reusable workflows from past trajectories (offline or online from self-generated successes) and stores them as memory for future pla (https://github.com/zorazrw/agent-workflow-memory, ★468, 2024-09)
- [paper] Teaching LLMs to Self-Evolve: MetaEvolve (UIUC) — Trains meta-skills (self-reflection, insight extraction from history, feedback integration) with GRPO on synthesized evolutionary trajectori (https://arxiv.org/abs/2607.21971, 2026-07)
- [repo] LLM4AD_Next / OpenLoopX (Optima-CityU) — One-command 'problem description -> runnable evolutionary algorithm search' platform: EoH, FunSearch, ReEvo, MEoH, MCTS-AHD, Diverse Island  (https://github.com/Optima-CityU/LLM4AD_Next, ★504, 2026-06)
- [repo] CodeEvolve (Inter / science-codeevolve) — Open-source evolutionary coding agent with island model, inspiration-based crossover, meta-prompting that lets the model rewrite its own ins (https://github.com/inter-co/science-codeevolve, ★144, 2025-10)
- [paper] Live-SWE-agent — Xia, Wang, Yang, Wei, Lingming Zhang (UIUC): agent starts with minimal bash tools and evolves its own scaffold on-the-fly at runtime while s (https://arxiv.org/abs/2511.13646, ★458, 2025-11)
- [repo] EvoAgentX — Open-source framework for evolving agentic workflows (TextGrad/AFlow/MIPRO optimizers) that accompanies the Fang survey; EMNLP 2025 demo. (https://github.com/EvoAgentX/EvoAgentX, ★3317, 2025-04)
- [paper] CORAL: Towards Autonomous Multi-Agent Evolution for Open-Ended Discovery — Open-source 'autoresearch' runner for Claude Code / Codex / OpenCode with grading, shared knowledge, and multi-agent evolution. (https://arxiv.org/abs/2604.01658, ★974, 2026-04)
- [paper] AlphaResearch: Accelerating New Algorithm Discovery with Language Models — Autonomous research agent looping propose -> program/verify -> optimize proposal, with execution reward plus simulated peer review; beats pr (https://arxiv.org/abs/2511.08522, ★61, 2025-11)
- [paper] WHALE: A Simple Recipe for Joint Harness-Weight Optimization — Alternates harness evolution and weight training in a joint loop (Yoonho Lee, Meta-Harness authors). (https://arxiv.org/abs/2609.00196, 2026-08)
- [paper] DemoEvolve: Overcoming Sparse Feedback in Agentic Harness Evolution — Che, Yang, Lin, Wang, Wang, Su: harness evolution around a frozen LLM as sample-efficient fast adaptation; uses expert demonstrations to gui (https://arxiv.org/abs/2605.24539, 2026-05)
- [repo] auto-harness — Bring-your-own-agent system that mines failures, optimizes the agent harness, and gates against regressions. (https://github.com/neosigmaai/auto-harness, ★536, 2026-04)
- [repo] Gödel Agent — Yin, Wang, Pan, Wan, W.Y. Wang (PKU/UCSB): LLM agent that dynamically modifies its own logic and code, including the code that analyzes and  (https://github.com/Arvid-pku/Godel_Agent, ★219, 2024-10)
- [paper] MOSS: Self-Evolution through Source-Level Rewriting — Argues that text-mutable artifacts (skills, prompts, memory, workflow graphs) cannot reach structural failures living in code, and performs  (https://arxiv.org/abs/2605.22794, 2026-05)
- [blog] Sakana AI Recursive Self-Improvement (RSI) Lab — Tokyo lab co-founded by Llion Jones dedicated to AI redesigning the AI development process; consolidates DGM, ShinkaEvolve, Digital Red Quee (https://sakana.ai/rsi-lab/, 2026-06)
- [repo] Live-SWE-agent — UIUC (Steven Xia): starts from bash-only mini-SWE-agent and synthesizes/refines its own tools and scaffold on the fly while solving tasks; 7 (https://github.com/OpenAutoCoder/live-swe-agent, ★458, 2025-11)
- [repo] Multi-Agent Evolve (MAE) — One base LLM instantiates Proposer, Solver, and Judge roles trained jointly via Task-Relative REINFORCE++ in a closed propose-solve-judge lo (https://github.com/ulab-uiuc/Multi-agent-evolve, ★157, 2025-10)
- [paper] Self-Play SWE-RL (SSR, Meta) — Bug-injection agent and bug-solving agent share one model and co-evolve via RL on sandboxed repos, no human issues or tests required; pitche (https://arxiv.org/abs/2512.18552, 2025-12)
- [paper] Socratic-SWE — Shared policy alternates Generator (repo-grounded repair task construction from solving traces) and Solver roles; self-evolving SWE agent. (https://arxiv.org/abs/2606.07412, 2026-06)
- [repo] Self-Rewarding Language Models (Meta/NYU) + lucidrains impl — Model generates its own prompts, responses, and LLM-as-a-Judge rewards, then iterates DPO on self-created preference data (arXiv 2401.10020) (https://github.com/lucidrains/self-rewarding-lm-pytorch, ★1411, 2024-01)
- [paper] AutoScientists: Self-Organizing Agent Teams (Zitnik lab) — Decentralized agents self-organize into teams around hypotheses, critique before spending compute, and share successes/failures across long- (https://arxiv.org/abs/2605.28655, 2026-05)
- [paper] POISE: From AI Assistant to AI Scientist - Autonomous Discovery of LLM-RL Algorithms — Fudan framework maintaining archives of proposals/implementations/evaluations to auto-discover policy-optimization algorithms; from GRPO, 64 (https://arxiv.org/abs/2603.23951, 2026-03)
- [paper] AIRA-Compose and AIRA-Design (Meta) — Two agentic neural-architecture-discovery frameworks: 11 agents produced 14 designs scaling to 3B params surpassing Llama 3.2 (54-71% faster (https://arxiv.org/abs/2605.15871, 2026-05)