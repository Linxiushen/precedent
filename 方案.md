# 自进化 Agent Harness — 最终方案（v1，2026-09-15）

> 本文档由 4 天调研（20 个发现智能体、96 份深读、6 份叙事、4 份缺口分析、6 个独立设计、13 份评审）+ 我本人的综合得出。所有数字均可在 `research/` 下的原始数据中核对。**结论先行，细节在后。**

---

## 0. 一句话结论

**做一个真正闭环的自进化 Claude Code harness——把别人都跳过的"接受"和"审计"两步做成核心。** 2026 年 9 月的现实是：提议者（proposer）到处都是（OpenClaw 389k★、DeepSeek Harness 222k★、Hermes 244k★、ECC 256k★、Claude Code 自动记忆、Codex Memories、Prime /refine、SkillOpt-Sleep…），而**决定"这次自我修改该不该保留"的接受者（acceptor）是整个领域公认的最弱环节**，且没有任何一个可运行的开源实现。五篇 2026 综述列出的四个开放问题（门控更新循环的参考实现、回归/保留协议、不自我确认的评估器共进化、多代基准）没有一篇附带代码。

我们的框架定位：**"给 agent 写给自己的状态做 CI/CD"** —— 面向用户的口径是"给你 agent 的大脑加上 git 和 CI"；面向学术的口径是"harness 级 RSI（递归自改进）的验证层"。

差异化核心（六个设计里唯一没有先例的两个想法）：
1. **每一次纠正都变成一个测试（Precedent）**：用户对 agent 的纠正是免费的、外生的、按用户标注的策略违规数据集，且违规动作已录在轨迹里——由此编译出的检查规则天生带有"种植的真值"（必须在被纠正的会话触发、在未纠正的会话安静），可以在确定性层级通过 hooks 强制执行，并且**复利**：人按一次确认，之后每个会话、每个候选修改、每次模型升级都免费受检。
2. **平局免费（ties are free）**：一个工件在某个历史会话里根本不可能被加载时，它在该会话的效果按构造就是平局；序贯检验只从"不一致对"学习，所以绝大多数效果≈0 的生产编辑可以**零模型调用**判定为"暂无效果"，真正需要花钱评估的只剩下会改变行为的候选。

---

## 1. 领域全景（为什么是现在、为什么是这个）

### 1.1 三个阵营，一个共同弱点
| 阵营 | 代表 | 有什么 | 缺什么 |
|---|---|---|---|
| A 生产型积累者 | OpenClaw、DSH、Hermes、ECC、OpenViking、Prime、GenericAgent | 真实用户、真实 harness、海量采用 | **零结果导向的适应度**；接受 = LLM 自己说了算。Hermes 学习循环在生产环境静默失效数周（41 次后台 fork、0 次更新，#95976）；Prime 把作弊漏洞固化成技能；OpenClaw 的评估器插槽默认 fail-open 且**零第三方实现** |
| B 研究型优化器 | DGM/HGM/HyperAgents/RQGM、Meta-Harness、Self-Harness、AHE、SkillOpt、SEA、PACE | 真基准适应度、归档、门控 | $500–22k/次、benchmark 绑定、统计粗糙（"2 次重复任何正增量即接受"）、七个评估器泄漏漏洞、跨运行不保留、代码常缺失（PACE/SEA/PROCTOR/HackProbe 无代码） |
| C 权重级自博弈 | AZR、Agent0、R-Zero、Dr. Zero | 零数据 | 多卡 RL，超出 harness 范畴 |

RSI 综述（2607.07663，1,250 篇）的"验证层级"是领域工作定律：**形式验证 > 执行反馈 > 学习型评判 > 自评**；"一个循环是改进还是原地打转，差别就在一级外部验证"。MetaRSI：69% 的 RSI 系统只在机器可检目标上闭环。

### 1.2 2026 夏季的批判浪潮（独立小组收敛）
- **归因**：AI2《Rethinking the Evaluation of Harness Evolution》——匹配预算下 harness 进化打不过朴素 test-time scaling，且泛化差（held-out 仅 +0.6）；Gideoni/Risi/Gal：平凡基线追平代码进化管线。
- **复利与保留**：《Do Agent Optimizers Compound?》一次性收益；EvoHarnessBench："harness 诱发的遗忘"（BWT −5/−4/−35%）。
- **谁受益**：Lin et al.《Harness Updating ≠ Harness Benefit》——9B 写的更新和 Opus 一样好，收益取决于消费方 agent 是否加载并遵循（加载率 25% vs 96%，遵循度 .52→.13）。
- **机制**：EvoTrace 30% 的新增行是被删代码的字节级重引入；Demystifying Skills 检索精度 29.6%→3.3%；CTA：LLM 写的技能效果 +0.3pp；**SkillsBench：LLM 写的技能 0 收益，人写的 +16.2**。
- **误进化**：SkillMisevo 21/21 进化配置产出不安全工件；MLAS：40/40 恶意载荷穿过 Hermes 后台审查持久化，OpenClaw 的人工队列 40/40 拦住。
- **接受者**：PACE——贪心"分数上升就保留"= 30–42% 误接受（我们的模拟复现为 44–46%）；HarnessDev：agent 自选版本在 held-out 上≈掷硬币（34/64），64 次进化编辑 0 次触碰验证器；RSI-Exam：所有前沿模型从可见集到密封集掉 12–25%。
- **评判者不能当最终权威**："More Convincing, Not More Correct"（判分 0.72→0.94 而准确率不变 0.20）；Blind Curator 悬崖；PROCTOR 规则：**机械拒绝覆盖 LLM 批准，永不反向**。

### 1.3 实践者视角（十个 issue tracker，~120 个 issue）——重排优先级
- 用户的词汇是**版本控制**，不是 RSI：diff / 收据 / 撤销 / 预算（Hermes 最高赞 #12238："给 ~/.hermes 做备份和版本控制"；Prime #1208 "/refine 是不可见的"；claude-code #82056 48 条评论"无法知道记忆是完整加载、截断还是没加载"）。
- **门控在生产里死于饿死而非过严**：write_approval 打开 8 周累积 241 条暂存写入无人知晓（#105770）。规则改写为：**通过时静默、阻止时响亮、饿死时绝不静默**。
- **最严重的事故是对"原本好的工件"的变异**（凌晨 2:17 后台 fork 把用户验证过的 chrome-devtools 技能改成"关闭所有 Chrome 窗口"，绕过账本直接写文件）。写权限必须按**文件路径**强制，不能按单个工具。
- 用户已经在要我们的机制：hermes#96704 RFC"配对双臂测量 agent 写的技能是否真的有用"；microsoft/SkillOpt#155"在一次性 worktree 里用真实 CLI 回放并按 diff 门控"（维护者两次背书，附三条硬约束：worktree 不是安全边界、结果必须 pass/fail/inconclusive、执行默认关闭）。
- 成本是静默杀手：Codex Memories 空闲时耗掉每周额度 40%，用户直接关掉。

### 1.4 底座接缝图（接缝已备好，评估器没人做）
| 底座 | 星 | 接缝 | 现状 |
|---|---|---|---|
| OpenClaw | 389k | `skill_proposal_evaluate(candidate bundle + baseline, tree sha256) → pass/revise/block`，文档明言"供外部优化循环" | **零第三方实现**；error/skipped 不阻止（fail-open） |
| DeepSeek Harness | 222k | 五阶段工具管道 + 可回滚插件效果 + 直接跑 Claude Code/Codex hooks.json | 作者"推迟评估器，直到其权限、隔离和 provider 契约被设计好" |
| Hermes | 244k | `~/.hermes/pending/{memory,skills}` 暂存队列、`pre_tool_call` 可 fail-closed | 默认关闭 |
| Claude Code | — | PreToolUse `updatedInput`/`permissionDecision`、PostToolUse **`updatedToolOutput`**（已实测可回放录音，$0.007/次）、InstructionsLoaded、`claude plugin eval`（六种 grader、with/without 差分、`history_file` 续跑、`scaffold_script`） | 记忆"是上下文，不是强制配置" |
| Reef | 1.3k | propose→paired-evaluate→decide 配方 API | 基准规格 #357/#358 开着，零评论 |

---

## 2. 为什么之前的直觉要修正（诚实记录）
- "没人做序贯检验"——**发明上错，采纳上对**。PACE（2606.08106）、SEA（2607.00871）已有；零框架使用。贡献 = 集成 + 扩展（多目标、逐任务底线、运行级误差预算、真实日志）。
- "没人做反事实回放"——错。Causal Agent Replay、CTA、SkillOpt-Sleep 有。且《The Replay Gap》证明：**磁带回放是行为变化/回归检测器，不是适应度函数**（早期干预后仅 3–8% 回放状态有效）。本机实测：2,062 次工具调用中 50% 是工作区写、20% 外部、13% 非确定性，幂等前缀 0–1——纯 VCR 回放在真实编码会话里立刻分叉，必须**沙箱优先**（git 快照里真实重执行 + 外部调用取录音 + 同配置对照臂给出噪声下限）。
- "Camp B 的七个泄漏是七个 bug"——是**一个系统 bug**：评估器位于提议者可写视图内。挂载命名空间 + 哈希绑定的分数收据可全部堵住。
- 评委补充的先例（设计里遗漏）：RewardHackingAgents（2603.11337，自发篡改率 ~50%、评估器锁定）、EvoUndo（2608.28363，可恢复层）、agnix（413★ 456 条 lint 规则）、cc-skill-usage、vercel-labs/skills（31.6k★ `npx skills add`）、Thru-Echoes/rsi-exam-provenance（verdict/disposition 分离）、Ratchet + Blind Curator（退役数学）。

---

## 3. 最终框架设计

### 3.1 名称（已定）
**Precedent**（"先例"：每一次纠正都是一条判例）。GitHub 名 `precedent`，包名 `precedent-cli`（PyPI/npm 的 `precedent` 已被占用）。许可证：**Apache-2.0**（建议；与 MIT 同为完全开源，多一条专利授权与 NOTICE 要求，更利于标准与企业采纳）。

### 3.2 定位（2026-09-15 按用户意见修订）
**Precedent 是一个自进化的 Claude Code harness。** 一个完整的自进化循环有五步：

| 步骤 | 含义 | 市面现状 | Precedent |
|---|---|---|---|
| ① 提议 | agent 改自己的记忆 / 技能 / 规则 / 工具 | 到处都是（Claude Code 自动记忆、skill-creator、Hermes 后台审查、Codex Memories、SkillOpt-Sleep…） | 三个提议者：(a) **纠正挖掘器**——从你的每一次纠正中编译规则；(b) Claude Code 自带的记忆/技能写入（纳入治理）；(c) 夜间**改进器**——从失败聚类提议技能/CLAUDE.md 修改（SkillOpt-Sleep 形态，但必须过门） |
| ② 接受 | 这次改动该不该留 | **没人做**（接受 = LLM 自己说了算；贪心 = 30–45% 误接受） | 出生门 + lint/断言/金丝雀 + 配对 e-process 门控 + plugin-eval 考试 |
| ③ 执行 | 改动生效并被真的用到 | 大量"写了但从未加载"（本机 41/60 从未被引用） | hooks 强制执行；加载收据证明到达模型 |
| ④ 审计 | 学习真的发生了吗、有没有变坏 | **没人做**（Hermes 静默失效数周无人察觉） | 漏斗告警、零流自校准、撤销、支出账本 |
| ⑤ 再进化 | 用审计结果调整提议者 | 无 | 被拒编辑回灌提议者；先例集随纠正持续生长 |

区别不在于"会不会改自己"，而在于**"改得对不对、能不能证明、能不能撤销"**。OpenClaw / DeepSeek Harness / Hermes 是后期的**分发渠道**（它们为②留了插槽，接上即可治理 389k★ 生态的自我修改），不是产品本身，也不在 3 个月核心范围内。

- **不是**：另一个记忆系统、另一个 skill 市场、另一个通用 agent 框架。Precedent 自身的评估器/规则集处于可变表面之外；对其修改是 L4/L5 事件，走人工合并分支。

### 3.3 分层架构（按用户需求顺序，而非按论文顺序）

**L0 收据与变更流（$0，无需任何学习循环，已在构建：`packages/receipts`）**
- 每会话每工件的**加载收据**：解析出的存储身份 + 哈希、可用字节 vs 实际载入字节、状态 `complete | truncated | not_loaded | wrong_store | missing_on_disk`。数据源：Claude Code 会话里的 `prompt_snapshot`（实际渲染的系统提示）、`skill_listing`、`<cc-memory filenames=…>` 引用标签、`Skill` 工具调用。直接回答 claude-code#82056 的三种不可区分失败。
- **变更流**："昨天以来什么变了、来自哪个会话、为什么"——Write/Edit/Bash 重定向对学习状态文件的每次变异，附前后 diff 与 agent 当时陈述的理由；带外变更标记为 actor unknown。
- **激活漏斗**：created → eligible → loaded → cited/invoked → last_modified；从不被引用的、近重复的、索引超上限被静默丢弃的条目。

**L1 快照、撤销、所有权、lint、预算**
- 每次无人值守写入前对整棵学习状态树做内容寻址快照；`undo --session X`（新增→删、更新→还原、删除→重建）带后续会话冲突检查与往返验证，深度 > 1。
- 写权限**按路径**强制（PreToolUse `permissionDecision` 对 memory 目录、CLAUDE.md、.claude/skills、settings hooks 的无人值守写入 → 暂存为候选）；用户验证过的工件默认对无人值守写入者不可变。
- 确定性 lint（零模型成本、通过时不可见）：frontmatter 可解析、引用的脚本存在、可执行技能可编译、标"verified"的声明必须引用工具输出片段、占位符（NNN/HH:MM）不得出现在 verified 块、会话专属事实（端口/主机名/绝对路径/日期）不得无 scope 标签进入全局规则、去重、DLP/密钥清洗、provider 边界。
- **每工件环境断言**（来自 Clade 设计）：写入时抽取机器可检的世界事实（二进制版本、路径、端口、主机、API 形状、仓库 commit 范围、OS）作为 `engines/peerDependencies`；每次加载探测，失败即 stale——**过期 = 一条断言失败**，不需要统计。
- 支出账本：按循环/按门控记账（token、缓存读、时长、美元），日上限、熔断器；漏斗告警：accepted=0 over N、activated=0、pending 队列超龄。

**L2 Precedent 引擎（差异化核心）**
1. **Miner**：从会话里挖掘用户纠正（否定/命令式紧跟 agent 动作、"我说过/又/不要/改用/重新"、git revert agent 的提交、重复指令）→ 归并为主题（本机 8 会话已见 ~12 条真实纠正、≥1 个重复主题、0 条被强制）。
2. **Compiler**：把主题编译成检查——优先确定性（Bash/Edit/Write 输入正则 → deny/ask/rewrite；文件 glob 后置条件），其次可执行（沙箱内测试），最后才是判据型（cross-family judge，仅可 ADMIT_PROVISIONAL）。
3. **出生门（birth gate，零模型成本）**：改编自 Double Ratchet 的探测器出生门与 SEA 的 fail-on-base：(a) 必须在 ≥2 个被纠正的会话上触发；(b) 在其有效的未纠正会话上触发 ≤ε（默认 2%，始终以计数展示如 9/9 vs 1/203）；(c) 在声明的层级可执行。
4. **Docket**：批量、推送、带 diff+证据+触发示例的一键确认/编辑/拒绝/推迟界面，14 天过期，绝不静默堆积。
5. **Enforcer**：`precedent hooks install claude-code` 写入 settings.json hooks（PreToolUse deny/ask/updatedInput、PostToolUse、Stop 阻止"未验证即宣称完成"）；OpenClaw 用 `before_tool_call`；Hermes 用 `pre_tool_call`；DSH 用 hooks.json 桥。
6. **Examiner**：把每个符合条件的历史会话编译为 `claude plugin eval` 用例（`scaffold_script` 恢复 git 快照 + `history_file` 续跑到分叉点 + 会话自带的验证命令作 grader + precedents 作 grader），用官方隔离双臂（with/without）评估任何候选技能/记忆/harness 变更/模型升级。

**L3 接受门控（统计引擎，已构建：`packages/acceptor`，65 测试通过，修补中）**
- 配对、anytime-valid 的 e-process 检验（PACE 的 betting martingale，支持固定 λ、混合、ONS、aGRAPA），只从不一致对学习；**平局免费**：由加载收据先判定工件在该会话是否可能被加载，不可能则该会话按构造为平局。
- **非对称决策**：对保护语料（现任 k/k 通过且观测稳定的任务）上的回归运行单独的"伤害"鞅（α_harm=0.10），几对不一致就能阻止；认证提升慢（α=0.05）；接受后在真实会话上继续累积证据（anytime-valid 允许跨天续跑），发现伤害自动回滚到已知安全检查点。
- 运行级误差预算：SEA 的无限期调度 δ_k = δ₀/(Z·k·log²(k+1))，Z = 3.3877（数值验证；朴素 1/(2k log²) 超支 1.69×）；10 个空候选的家族错误率从 15–18% 降到 0.6–1.0%。
- 逐任务底线只对观测稳定的任务启用并 k-of-n 确认（模拟：Bernoulli(0.6) 任务上 k=1 误拦 93–100%，k=3 仍 13–43%）。
- 多目标约束先于统计：token/时延/harness 体积增长上限、安全金丝雀通过率、激活率——机械拒绝优先。
- **零流自校准**（来自 Turnstile）：喂给门控字节相同/仅空白变化的候选（A vs A′），任何接受按构造即误接受——每个部署的门控都能免费报告自己的经验误接受率；"幻影接受"（接受了但从未被加载）作为混淆矩阵的一格。
- 诚实预期：二值信号下 40 个任务只能可靠检出 ≥+30pp 的提升（+10pp 需 160+）。这与"生产编辑大多≈0 效果"一致——门控"免费拒绝"大多数编辑是**正确行为**；产品必须以收据/回归/伤害（高信号）为主打，把"认证提升"作为慢速后台过程。

**L4 密封评估与收据（来自 Hermetic，诚实缩窄）**
- 分数收据 = sha256(评估器, 隐藏数据, 候选, 最终工件) + 三态结果（valid / invalid-artifact / infra-error）+ 原因码；自报告永远不是打分输入（HarnessDev）；fail-closed 归零（RSI-Exam）。
- 评估器与隐藏数据位于提议者文件系统视图之外（Linux 挂载命名空间/容器；macOS 退化为独立用户 + sandbox-exec，**明确标注为较弱边界**）。
- 评委指出的限制：进程内的"执行证据探针"可被伪造，蜜罐令牌只能检测幼稚泄漏（若提议者读不到评分器，蜜罐更像自检）。因此 v1 只承诺**结构性隔离 + 收据可离线验证**，不承诺"不可伪造"。

**L5 研究轨道（论文）**
- **《Every Correction Is a Test》**：C1 可编译性与幻影率——在 ≥15 位知情同意用户的 Claude Code/Hermes 日志（≥3,000 会话）上，≥25% 的纠正属于重复主题，≥50% 的重复主题可在 ε=2% 下通过出生门编译为确定性/可执行检查，而 LLM 自提议的护栏（Phantom Guardrails）幻影率显著更高；C2 "落地的汇率"——每条被确认的先例在后续会话减少的同主题纠正数；C3 零流校准——把领域已发布的接受者（SkillOpt、Self-Harness、Reef、GEPA、Hermes 审查、Prime refine）接到零流上，报告其经验误接受率与幻影接受率。
- 基准以 **Harbor 任务族**发布（13 个相关项目已用 Harbor；Reef #447 正把记录转成 Harbor 任务），自动继承所有适配器与排行榜。

### 3.4 数据契约（JSON，简式）
```json
// 加载收据
{"schemaVersion":1,"session":"…","artifact":"memory/project-goal.md","sha256":"…","bytes_on_disk":3139,"bytes_loaded":3139,"status":"loaded_complete","evidence":{"record":"attachment/prompt_snapshot","ts":"…"}}
// 候选（HarnessPatch，参考 MetaRSI/AHE）
{"candidate_id":"sha256:…","surface":"skill|memory|claude_md|hook|tool","declared_paths":[…],"diff_sha256":"…","hypothesis":"…","expected_effect":"…","provenance":{"origin":"unattended_review|foreground|user|hub","taint":["tool_output","web"]}}
// 先例
{"id":"…","topic":"use uv not pip","checks":[{"rung":"deterministic","hook":"PreToolUse","match":"Bash","input_regex":"^pip install"}],"scope":"project","authority":{"confirmed_by":"user","at":"…"},"birth":{"fires_on_corrected":9,"fires_on_uncorrected":1,"eligible_uncorrected":203},"status":"active"}
// 证书/账本条目（SEA 模式，哈希链）
{"candidate_id":"…","round":3,"algorithm":"paired-eprocess-mixture","decision":"ACCEPT|HOLD|REJECT|NSF|BLOCKED","alpha_spent":0.0147,"cumulative_alpha":0.031,"metrics":{"discordant":7,"wealth":21.3,"bwt":0.0,"fwt":0.12},"verification_rung":"execution","closure":"human_on","prev_hash":"…","hash":"…"}
```

### 3.5 安全模型（一句话版）
接受 = 机械检查（lint/DLP/所有权/断言/金丝雀）→ 统计（配对 e-process）→ 人（仅 HOLD 需要人）；LLM 评判永远不能单独把候选推到 VERIFIED；agent 创建的工件视为不受信任的社区代码（来源不提升信任，MLAS 教训）；提议者永远看不到评分器；每次无人值守写入前有快照；每条决策有哈希链证书。

### 3.6 五分钟演示（诚实版，前 4 分钟零模型调用）
1. `pipx install precedent-cli && precedent init` —— 10 秒：`发现 N 个会话、M 个学习工件。MEMORY.md 在最近 12 个会话里 9 次被截断，最新 6 条（你周二的纠正）从未到达模型。K 个技能 60 天内从未加载。上周 18 次无人值守写入，1 次绕过了技能簿记。`
2. `precedent mine` —— `你纠正了 agent N 次，M 个主题重复；其中 K 条已经写在 CLAUDE.md 里、但在加载了 CLAUDE.md 的会话中仍有 40% 被违反；0 条被强制执行。`
3. `precedent docket` —— 一键确认最重复的 3 条先例，看到出生门证据（9/9 触发 vs 1/203 误触发）。
4. 开一个新 Claude Code 会话，让 agent 做一件会违反先例的事 —— PreToolUse 拦截，附上先例 id 与当初的纠正原文。
5. （可选，需 API）`precedent examine --candidate skills/foo` —— 用 `claude plugin eval` 双臂跑 6 个历史用例，输出 pass/fail/inconclusive、A-vs-A′ 噪声下限、以及"暂无效果"的诚实结论。

---

## 4. 范围纪律（评委一致的致命缺陷：所有方案都是 1–3 人能力的 3–5 倍）

**3 个月只做一件事：在 Claude Code 上跑通完整的、经验证的自进化闭环**（①提议 → ②接受 → ③执行 → ④审计 → ⑤再进化），以你自己的机器为第一个部署。**不做**：OpenClaw/DSH/Codex/pi/Reef 适配器（第 4 个月起作为分发渠道）、联邦/技能中心、标准提案（仅留草案）、GPU。

| 周 | 交付 | 闭环步骤 |
|---|---|---|
| 1–2 | 合并 `receipts` + `acceptor` 为 `precedent` CLI（`init / scan / undo / hooks install`）；快照 + 撤销；lint/DLP；支出账本；观察模式 v0.1 | ③④ |
| 3–4 | **纠正挖掘器 + 编译器**（确定性检查）+ 出生门 + docket + hooks 强制；本机跑通"纠正 → 规则 → 下次拦截"；≥5 位志愿者试用；回帖 claude-code#82056/#75334、hermes#96704、SkillOpt#155 | ①②③ |
| 5–6 | 治理 Claude Code 自带的记忆/技能写入（路径级所有权、暂存为候选）；环境断言 + staleness；门控接入（零流自校准、伤害鞅、平局免费）；examiner v0（磁带 → plugin eval 用例） | ②④ |
| 7–8 | **夜间改进器**（失败聚类 → 提议技能/CLAUDE.md 修改 → 过门 → 晋升/回滚）；沙箱优先回放 v0（按 SkillOpt#155 三条约束：容器 + 隔离凭据 + 无网络；tri-state；默认关闭）；被拒编辑回灌提议者 | ①⑤ |
| 9–10 | 论文实验 C1/C2（志愿者日志）+ C3（零流接入 6 个已发布接受者）；多代（G≥10 晚）自进化曲线：保留（BWT）/ 适应（FWT）/ 安全 / 成本 | ④ |
| 11–12 | Harbor 任务族 + 排行榜 + v1.0 + 论文投稿（ICLR RSI workshop 线）；第 4 个月起：OpenClaw `skill_proposal_evaluate` 插件、DSH hooks 桥 | 分发 |

评委的其余致命点如何处理：
- **本机没有带测试命令的会话，回放式提升认证无数据** → 演示不承诺回放数字；先例/收据是主打；回放作为可选第 5 分钟并明示 inconclusive。
- **vendor 会吸收 L0（Anthropic 一行警告就能解决截断提示）** → L0 是入口不是护城河；护城河是先例引擎 + 门控 + 零流校准 + 跨底座。
- **INCONCLUSIVE 为主的产品会被当成没结果** → 非对称设计：回归与伤害快速可证、提升慢速认证；把"暂无效果"作为一等公民展示（这本身就是 null-effect 文献的结论）。
- **lint 已商品化（agnix 等）** → 直接依赖/复用 agnix 规则，不自研 lint 引擎。
- **名字冲突** → 见 3.1。

---

## 5. 已交付物（截至 2026-09-15 上午）
- `research/landscape.json`（521 索引、64 深读、批评家分类学）、`landscape_digest.md`
- `research/rsi.json` / `rsi_digest.md`（6 叙事 + 32 深读）
- `research/gap_analyses_all.json`（科学家 / 安全 / 实践者 / 生态 四个视角）
- `research/synthesis_v0.md` → `v1.md` → `v2_rsi.md`（假设、纠正、实测数字、接缝契约）
- `research/designs_partial.json` + `design_results.json`（6 设计 + 13 评审）
- `research/tools/cassette_extract.py`（会话 → 磁带 + 可回放性分类）、`gate_power_sim.py`
- `packages/acceptor/` v0.2.0（5,184 行，**369 测试通过**）：门控库——配对 e-process ×4 策略、SEA 调度 Z=3.3877、逐任务底线（三态 verdict）、多目标约束、哈希链账本 + 候选监视器（拒绝回放/篡改/倒退）、实例采样器（提议者不能挑选顺序）、接受者基准 CLI（planted / stochastic / **adversarial** 三个体制）。实测：贪心 44.6% 误接受 vs 门控 1.4–3.1%；对抗攻击（重排/回放/挑选/重提议）从 78–100% 降到 0–2%
- `packages/receipts/` v0.1（3,964 行，**71 测试通过**）：加载收据 + 变更流 + 激活漏斗，只读，本机 8 会话 4.4 秒。**在你机器上的真实发现**：60 个学习工件中 **41 个从未被引用**；4 组近重复技能（lark-vc / lark-vc-agent / lark-minutes / lark-note，描述 Jaccard 0.91–0.97）；`microcat-project.md` 经 32 次写入长到 20 KB，其中 31 次是 Bash heredoc 绕过记忆工具（hermes#99729 那类"按工具设门、按路径绕过"在野外的实例）；一个项目的 MEMORY.md 在会话中途被截断（新增索引行未到达模型）；2 条索引指向不存在的文件；0 条无法解释的带外变更。三条主张已用 `sed -n`/`grep` 对照原始轨迹逐条核实。额外发现更权威的数据源：`instructions` 附件带逐文件加载记录、`file-history-delta` 可佐证写入、`attributionSkill` 是额外的引用信号
- **`packages/precedent/` v0.1.0**（12,712 行，**1,060 测试通过**，Apache-2.0）：`precedent` CLI——`init` / `scan` / `mine`（纠正挖掘 v1）/ `compile`（6 个确定性模板 + **时序出生门 v1**：t0 之后的 HIT / QUIET-AFTER / pre-t0 只报告不计入）/ `confirm` / `own` / `docket` / `hooks install|uninstall|status`（六钩套件，默认 dry-run；dry-run 现在会打印**它将使用的确切备份路径**和**settings.json 的精确 unified diff**）/ `snapshot` / `undo --session` / `examine` / `improve` / `loop` / `report`；哈希链账本复用 `acceptor`。每条命令都有“假 home 字节级不变”测试，外加 `real_home_canary` fixture。**本机真实结果（2026-09-15 E-Prepare，逐字记录见 `DEMO.md`）**：165 条人类输入（已剔除 119 条 skill/命令展开）→ 20 条纠正（12.1%）→ 18 个主题 → **1 个重复主题**；为“不要开无头浏览器”编译的 `dont_use headless` 规则在 v0 门下被拦（2/2 vs 2/5），在**时序出生门**下 **PASS**（t0 后 32/8164 = 0.4% ≤ ε，t0 前 33/945 只报告）——这正是时序修正要解决的错误。而 README 里当例子的 `require_field model=opus` 规则**两种编法都没过门**：`Agent|Workflow` 版 QUIET-AFTER 4/8 = 50%（`Workflow` 工具根本没有 `model` 字段，29/29 次调用都没有），`Agent` 版 QUIET-AFTER 完美 0/4 但没有 HIT（用户当时打断的是一次 `Workflow` 调用）——**前瞻性策略没有“被纠正的动作”可绑定，是 v1 门的真实局限**。确认 1 条、强制执行 0 条；`~/.claude` 下 1,625 个文件运行前后指纹对比，`settings.json` sha256 不变。
- `scratchpad/replay-test/`：Claude Code hooks 回放机制验证（PreToolUse updatedInput + PostToolUse updatedToolOutput）
- **`DEMO.md`**：E-Prepare 阶段的演示实录——每条命令 + 逐字输出、时序出生门的全部计数、`hooks install` dry-run 的备份路径与精确 diff、在一次性假 home 上的 deny/allow 端到端验证（含 20 次真实工具调用 0 拦截）、用户运行手册（三条命令 + 一行卸载）、以及 `~/.claude` 前后指纹审计（每个变动文件都有归因，两次对照实验区分“precedent 写的”和“`claude -p` 子进程写的”）

## 5.1 闭环已跑通（2026-09-16）

仓库：**https://github.com/Linxiushen/precedent**（私有，Apache-2.0）。CI 8/8 全绿（3.11 + 3.12 × 三个包，加 extras 降级与 wheel 构建）。测试：**receipts 71 · acceptor 112 · precedent 639**。

五步闭环在你自己的机器上各自有了真实证据：

| 步 | 证据 |
|---|---|
| ① 提议 | 挖掘器从 165 条人类输入里挖出 20 条纠正、18 个主题；改进器从 26 个失败签名聚类起草修改（全部过 schema + `diff == declared_paths` + lint） |
| ② 接受 | 出生门在真实数据上**三次拒绝**：全局 headless 规则（会打断 68 次合法操作）、`require_field model=opus`（Workflow 根本没有 `model` 字段）、`require_regex`（另一项目确有不合规）；本项目范围内 0/2 误触但**证据不足**（门控要求 ≥3），规则留在候选里 |
| ③ 执行 | 六个钩子 + 假 home 上端到端验证：违规 → deny（附先例 id 与你当初的原话），合规 → allow，20 次随机真实调用 → 0 拦截，卸载后 `settings.json` 字节一致 |
| ④ 审计 | 漏斗、饿死告警、哈希链账本、支出表；退役规则单列，不计入"已强制执行" |
| ⑤ 再进化 | 被拒草稿进 `rejected.jsonl`，下一轮作为负例喂回提示词 |

**门控找到了一个真 bug（而不是我读代码读出来的）**：`require_regex` 在真实语料上报出 4/4 = 100% 误触，但其中两次调用的脚本**确实**写了 `model: 'opus'`——在第 5,778 和 5,659 字符处，超过了 4,096 字符的输入截断上限。正则没看到它，"没匹配"被当成"违规"，这条规则会**拦截合规的调用**。截断是"不知道"的第三种形式（另外两种是正则被隔离、输入不是文本），反向匹配器必须对三种都 fail-open。修复后 4/4 → 1/4。门控拒绝一条坏规则的同时，顺手交出了 bug 报告。

**唯一需要你亲手运行的一步**在 `DEMO.md` §6（三条命令 + 一行卸载）。分类器不允许我修改自己正在运行的 Claude Code 配置，所以 `hooks install --apply` 由你来执行。

## 6. 决策记录（2026-09-15）
1. **名称**：Precedent ✅
2. **语言**：Python 核心 CLI（JSON stdin/stdout，命令式 hook 形态）；分发渠道插件（OpenClaw 等）第 4 个月起用 TypeScript 薄壳调用 CLI
3. **许可证**：Apache-2.0 ✅（2026-09-15 用户全部确认）
7. **强制执行的启用**：把钩子写入 `~/.claude/settings.json` 这一步由**用户亲手运行**（`precedent hooks install claude-code --apply`）；助手自身受自动模式分类器限制不能修改自己正在运行的 Claude Code 配置，因此只做 dry-run、假 home 端到端验证，并把运行手册写进 `DEMO.md`
4. **首发范围**：3 个月 = Claude Code 上的完整自进化闭环；OpenClaw 等移出核心范围 ✅
5. **数据**：`research/` 随仓库开源 ✅（发布前需校对引用）
6. **网页**：已发布 https://claude.ai/artifact/STVtU54PzQ5pfPqr5BMoaT ✅

---

## 附录 A：关键来源（可在 `research/` 中检索）
PACE 2606.08106 · SEA 2607.00871 · Causal Agent Replay 2606.08275 · The Replay Gap 2608.08239 · CTA 2605.11946 · AI2 Rethinking 2607.12227 · Lin et al. 2605.30621 · SkillMisevo/SafeEvolve 2608.12851 · MLAS 2606.23075 · EvoHarnessBench 2609.04280 · HarnessDev 2609.01437 · RSI survey 2607.07663 · MetaRSI 2609.06396 · RewardHackingAgents 2603.11337 · EvoUndo 2608.28363 · Grounding Agent Memory 2609.11060 · CLAUDE.md Catastrophic Remembering 2608.11095 · Hermes #95976/#96704/#105770/#12238 · claude-code #82056/#92998/#75334 · SkillOpt #154/#155/#174 · Reef #356–358 · OpenClaw `src/skills/workshop/` · Claude Code hooks & plugin-evals docs（2026-09）。
